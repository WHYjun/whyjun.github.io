var store = [{
        "title": "Go에서 HashSet 구현하기",
        "excerpt":"12월 18일 기준 가장 최근 버전인 Go 1.17에도 아직 Set이라는 자료구조가 없다. 알고리즘 문제나 Advent of Code 문제를 풀 때 Set을 필요로 하는 경우가 생각보다 많았어서 직접 구현해보았다. Set이라는 자료구조를 통해 얻고 싶어하는 기능들을 아래와 같다.   데이터를 중복 없이 관리하고 싶을 때  데이터를 쉽게 추가하고 제거하는 기능이 필요할 때  데이터를 적은 공간 복잡도를 가진 자료구조에서 관리하고 싶을 때그리고 이러한 기능들은 Go 언어에서 제공하는 map을 사용하면 아래와 같이 간단하게 구현할 수 있다. type set map[interface{}]struct{}실제로 사용할 때는 interface{}를 추가하고 싶은 데이터의 자료형으로 변경해서 사용하면 된다. (e.g: type set map[string]struct{}) Go에서 map은 HashTable을 구현해놓은 것이기 때문에 key는 중복될 수 없고 데이터를 추가하고 제거하는 것은 O(1)으로 매우 빠르다. 그리고 빈 struct{}는 0바이트이기 때문에 메모리 사용도 최소화할 수 있다. 만약 위의 타입을 그냥 사용하는 것이 다른 동료 개발자들을 헷갈리게 할 것 같다면 아래와 같이 set에 함수들을 추가하는 것으로 해결할 수 있다. func (s set) Add(v interface{}) {\ts[v] = struct{}{}}func (s set) Remove(v interface{}) {\tdelete(s, v)}func (s set) Contain(v interface{}) bool {\t_, ok := s[v]\treturn ok}func (s set) Length() int {\treturn len(s)}위의 코드들은 Go Playground에서 직접 사용해보실 수 있다. ","categories": ["Golang"],
        "tags": ["golang","data structure"],
        "url": "https://whyjun.github.io/golang/Write-Hashset-In-Go"
      },{
        "title": "Go에서 Enum 사용하기 그리고 주의할 점",
        "excerpt":"Go 언어는 enum 타입을 제공하지 않지만 Enum으로 사용할 데이터 타입을 따로 생성 후 iota를 이용해서 비슷하게 사용할 수 있다. (예시 코드) package mainimport \"fmt\"type level inttype Status intconst (\teasy level = iota + 1\tmedium\thard)const (\tStatusUnknown Status = iota\tStatusOK\tStatusError)func main() {\tlevelList := []level{easy, medium, hard}\tfmt.Println(levelList)\t// Output: [1 2 3]\tstatusList := []Status{StatusUnknown, StatusOK, StatusError}\tfmt.Println(statusList)\t// Output: [0 1 2]}주의할 점은 iota가 항상 0 부터 시작하기 때문에 enum의 첫 값을 iota + 1으로 해서 1 부터 시작하도록 하던지 아니면 첫 enum을 Unknown이나 Nil을 나타내는데 사용해야 한다. Go의 Zero Value때문에 예상치 못한 부작용이 생길 수 있기 때문이다. 이벤트 큐에서 아래와 같은 JSON 형식의 request를 받는 시스템이 있다고 가정해보자. type Request struct {\tID        int    `json:\"ID\"`\tTimestamp int    `json:\"Timestamp\"`\tMessage   string `json:\"Message\"`\tStatus    Status `json:\"Status\"`}만약 이 Request를 보내는 코드에서 아래와 같이 Status를 JSON에 넣는 것을 까먹고 보냈다면 어떻게 될까? {  \"ID\": 1,  \"Timestamp\": 1641775651,  \"Message\": \"Example message\"}Status 필드에 아무런 데이터가 들어가있지 않기 때문에 Request의 Status 값은 int의 Zero Value인 0이 될 것이다. 첫 enum이 StatusUnknown이 아니라 StatusOK였다면 Status 데이터가 없는 모든 Request들은 아무도 모르게 버그를 일으킬 것이다. 그렇기 때문에 Go에서 enum을 사용할 때는 Zero Value를 염두에 두고 꼭 첫 enum을 Unknown이나 Nil을 나타내는 값으로 지정하는 습관을 가져야 한다. ","categories": ["Golang"],
        "tags": ["golang"],
        "url": "https://whyjun.github.io/golang/Enum-in-Golang"
      },{
        "title": "golangcli-lint로 Go 코드 규칙 적용하기",
        "excerpt":"개발팀 사이즈가 커지면서 관리해야 하는 리포지토리의 개수가 늘어났고 각각의 프로젝트가 팀을 리드하는 개발자에 따라 자유분방한 모습으로 진화했습니다. 특히 Go 언어를 처음으로 사용하는 개발자들이 늘어나면서 코드 리뷰 과정에서 Idiomatic Go에 대해서 설명하고 작성하는데 시간을 꽤 소모하게 되었습니다. 그러다보니 실제로 리뷰가 되어야 하는 부분보다 더 많은 시간을 쏟게 되는 경우가 늘어났고 이를 방지하기 위해 자동화된 도구가 필요하다라는 피드백이 나오기 시작했습니다. 다행히도 Go 커뮤니티에는 다양한 Lint 도구들이 이미 개발되어 있었는데 대표적으로 govet, gofmt, golint 등이 있습니다. 이러한 도구들은 좋은 Go 코드를 작성하기 위해 필요한 부분들을 다 체크하지만 각각 따로 돌려야 한다는 단점이 있습니다. 이러한 단점을 극복하기 위해 저희 팀은 golangci-lint를 사용하기로 했습니다. golangci-lint는 Lint 도구는 아니지만 다양한 Lint 도구들을 한번에 돌릴 수 있게 도와줍니다. 단순히 도구들을 순서대로 돌리는 것이 아니라 병렬적으로 돌리는 동시에 분석 결과값을 캐싱함으로써 정적 분석을 매우 빠르게 끝낼 수 있습니다. 그리고 회사에 많은 개발자들이 사용하고 있는 Visual Studio Code를 지원하고 이 점을 활용해 settings.json에 설정 몇 가지를 추가하는 것으로 코드 규칙을 지키지 않을 경우 Warning 메세지를 바로 띄워주어 개발자들이 빠르게 고칠 수 있게 도와줍니다.  설치하기 Mac과 Windows가 아닌 다른 환경에서 설치하는 방법은 공식 문서에 자세히 나와있습니다. Mac brew install golangci-lintbrew upgrade golangci-lintgolangci-lint versionWindows # Git for Windows를 설치할 때 같이 설치되는 Git Bash에 아래 명령어를 입력하시면 설치됩니다.# 실제 binary는 $(go env GOPATH)/bin/golangci-lint에 있습니다curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin golangci-lint --version사용하기 golangci-lint run위 명령어를 입력하면 golangci-lint 기본으로 설정되어 있는 Lint 도구들로 코드를 분석합니다. 어떠한 도구들을 사용하는지 확인하고 싶으면 golangci-lint help linters로 확인할 수 있습니다. golangci-lint를 직접 설정하고 싶다면 공식 문서를 참고해서 .golangci.yml 파일을 작성하시면 됩니다. Visual Studio Code에 추가하기 Visual Studio Code 설정에 들어가서 settings.json을 연 다음, 아래 설정을 추가하면 Visual Studio Code에서 Warning 메세지를 확인할 수 있습니다. \"go.lintTool\": \"golangci-lint\"\"go.lintFlags\": [    \"--fast\"]사용 후기 실제로 적용해보니 코드 리뷰로 잡아내지 못한 이슈들이 꽤 있었습니다. 대표적으로 error를 fmt.Errorf로 래핑할 때, %w를 사용하지 않았던 부분입니다. errors 패키지를 잘 활용하지 않았다보니 알아차리지 못했지만 프로젝트의 사이즈가 커지면서 어떤 error인지를 확인해야 하는 경우가 빈번하게 발생했는데 %w가 아닌 %s나 %v를 사용했을 때는 errors.UnWrap() 함수를 사용할 수 없고 그 때문에 errors.Is와 errors.As도 활용할 수 없다는 것을 알게 되었습니다. 이처럼 Linter는 하나의 정해진 코드 규칙을 모든 개발자가 지킬 수 있게 해준다는 점을 넘어서 코드에 있는 버그들을 개발자들이 일찍 알아차릴 수 있게 해줍니다. ","categories": ["Golang"],
        "tags": ["golang"],
        "url": "https://whyjun.github.io/golang/Use-GolangCI-Lint"
      },{
        "title": "Go에서 omitempty 태그 사용하기",
        "excerpt":"Go로 웹 서버를 개발하다 보면 JSON 데이터를 주고 받는 경우가 많이 있습니다. struct를 JSON으로 바꿀 때 값이 Zero Value(각 type별 기본 값)이거나 nil인 경우 그 값을 보내지 않아야할 때가 있습니다. 이런 경우, omitempty 태그를 활용하면 JSON에서 쉽게 생략할 수 있습니다. struct 필드에 omitempty 태그가 있다면, Go 언어의 JSON 인코더는 그 필드의 값이 Zero Value거나 nil일 때 그 필드를 생략합니다. 이는 JSON으로 데이터를 보내고 받는 API를 개발할 때 유용하고 특히 JSON 메세지의 사이즈를 줄일 수 있다는 장점이 있습니다. omitempty 태그 struct의 omitempty 태그는 아래와 같이 필드 type 뒤에 Backtick(`)안에 작성해 추가할 수 있습니다. type Person struct {    Name      string `json:\"name,omitempty\"`    Age       int    `json:\"age,omitempty\"`    Address   string `json:\"address,omitempty\"`    Telephone string `json:\"telephone,omitempty\"`}위의 예시를 보면 모든 필드에 omitempty 태그가 있는 것을 볼 수 있습니다. 이는 필드의 값이 Zero Value라면 JSON 형식으로 데이터를 변경할 때 필드가 사라짐을 의미합니다. 예를 들어 아래의 값을 가진 Person 인스턴스가 있다고 합시다. p := Person{    Name:      \"John\",    Age:       0,    Address:   \"\",    Telephone: \"123-456-7890\",}Person을 json.Marshal 함수를 사용해서 JSON으로 바꾼다면 결과값은 아래와 같이 나옵니다. {    \"name\": \"John\",    \"telephone\": \"123-456-7890\"}위에서 볼 수 있듯이, Age와 Address 필드가 사라진 것을 확인할 수 있습니다. ","categories": ["Golang"],
        "tags": ["golang"],
        "url": "https://whyjun.github.io/golang/Golang-Omitempty-Tag"
      },{
        "title": "The Importance of Signals from Test Codes",
        "excerpt":"When you write tests, they tell you a lot about your main code. If a test is hard to write, the problem is usually in the code you are testing, not in the test. Those difficulties are signals, and they are worth reading. Why Test Codes Matter Tests are a mirror for your main code. They show its strengths and its weak spots. A few signals to watch for:   Difficulty writing tests: the code might be too complex or not modular.  Excessive mocking: the code might be tightly coupled.  Long setup times: the code might carry too many dependencies.  Hidden dependencies: the code might be hard to maintain or extend.Key Signals and What They Mean Excessive Mocking Mocking replaces a real dependency with a fake one. A few mocks are normal. When a single test needs many of them, the class under test leans on too many other classes. The fix is in the design: break the large class into smaller ones that can stand on their own. Long Setup Times Setting up a test should be quick. When it drags, the code usually has too many dependencies or too little structure. Dependency injection helps here, since the test can supply what it needs instead of building the whole world first. Difficulty Isolating Tests Each test should run on its own. When one test changes the outcome of another, the code has shared state hiding somewhere. Make each unit do one thing so the tests stop leaking into each other. Hidden Dependencies Some dependencies are not visible in a method’s signature. Calling Instant.now() inside a method is one of them: the method quietly depends on the system clock, so you cannot test it without controlling time. Dependency inversion fixes this. Instead of reading the clock directly, inject a clock interface and ask it for the current time. Implementing Dependency Inversion Here is the same idea in code. Before: public class Event {    private Instant timestamp;    public Event() {        this.timestamp = Instant.now();    }}After: public interface Clock {    Instant now();}public class SystemClock implements Clock {    public Instant now() {        return Instant.now();    }}public class FakeClock implements Clock {    public Instant now() {        return Instant.parse(\"2007-12-03T10:15:30.00Z\");    }}public class Event {    private Instant timestamp;    public Event(Clock clock) {        this.timestamp = clock.now();    }}Benefits of Listening to These Signals Reading these signals improves the design. Modular code is easier to follow, has fewer places for bugs to hide, and takes less effort to extend when the requirements change. Practical Steps to Improve Your Code   Refactor regularly. Don’t wait until the code is a mess. Clean it up as you go.  Use dependency injection. It reduces the need for mocks and keeps the code modular.  Write small, focused functions. Each one should do a single thing, which makes it easy to test.  Find and remove hidden dependencies. Patterns like port-adapter and dependency inversion decouple your code.Conclusion Tests do more than check that your code works. They point at where the design is weak. When a test is painful to write, listen to it and fix the code underneath instead of forcing the test around it. Keep your tests simple, and let them guide the design. ","categories": ["development"],
        "tags": ["test"],
        "url": "https://whyjun.github.io/blog/The-Importance-of-Signals-from-Test-Codes"
      },{
        "title": "Using Interface for Controller Requests in Java Spring Framework",
        "excerpt":"When you build a REST API with Spring, one endpoint often has to accept several kinds of request. Using an interface for the request body lets a single endpoint handle them all. The example here is a restaurant order system, where one /v1/orders endpoint takes both hamburger and pizza orders. Scenario: Restaurant Order System We have an endpoint /v1/orders that accepts different order types. Every order shares a few fields and adds its own. We define an Order interface and implement it with one class per order type. Step 1: Define the Order Interface Start with an Order interface holding what every order has in common. public interface Order {    String getOrderType();}Step 2: Implement Order Types Then write a class per order type, like HamburgerOrder and PizzaOrder. import lombok.Data;import javax.validation.constraints.NotNull;@Datapublic class HamburgerOrder implements Order {    @NotNull    private String orderType = \"hamburger\";    @NotNull    private String bunType;    @NotNull    private String meatType;    private boolean extraCheese;}import lombok.Data;import javax.validation.constraints.NotNull;@Datapublic class PizzaOrder implements Order {    @NotNull    private String orderType = \"pizza\";    @NotNull    private String crustType;    @NotNull    private String size;    private boolean extraToppings;}Step 3: Create the Controller The controller handles the /v1/orders POST request. The @RequestBody and @Valid annotations bind and validate the incoming JSON. import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.validation.annotation.Validated;import org.springframework.web.bind.annotation.*;import javax.validation.Valid;@RestController@RequestMapping(\"/v1/orders\")@Validatedpublic class OrderController {    @PostMapping    public ResponseEntity&lt;String&gt; createOrder(@Valid @RequestBody Order order) {        if (order instanceof HamburgerOrder) {            HamburgerOrder hamburgerOrder = (HamburgerOrder) order;            // Process hamburger order            return new ResponseEntity&lt;&gt;(\"Hamburger order received\", HttpStatus.OK);        } else if (order instanceof PizzaOrder) {            PizzaOrder pizzaOrder = (PizzaOrder) order;            // Process pizza order            return new ResponseEntity&lt;&gt;(\"Pizza order received\", HttpStatus.OK);        } else {            return new ResponseEntity&lt;&gt;(\"Unknown order type\", HttpStatus.BAD_REQUEST);        }    }}Step 4: Configure Jackson for Polymorphic Deserialization Jackson needs to know which class to build from the JSON. Annotate the Order interface so it reads the orderType field and picks the matching implementation. import com.fasterxml.jackson.annotation.JsonSubTypes;import com.fasterxml.jackson.annotation.JsonTypeInfo;@JsonTypeInfo(        use = JsonTypeInfo.Id.NAME,        include = JsonTypeInfo.As.PROPERTY,        property = \"orderType\")@JsonSubTypes({        @JsonSubTypes.Type(value = HamburgerOrder.class, name = \"hamburger\"),        @JsonSubTypes.Type(value = PizzaOrder.class, name = \"pizza\")})public interface Order {    String getOrderType();}Step 5: Add Global Validation Handling Finally, handle validation errors in one place. import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.MethodArgumentNotValidException;import org.springframework.web.bind.annotation.ControllerAdvice;import org.springframework.web.bind.annotation.ExceptionHandler;@ControllerAdvicepublic class GlobalExceptionHandler {    @ExceptionHandler(MethodArgumentNotValidException.class)    public ResponseEntity&lt;String&gt; handleValidationExceptions(MethodArgumentNotValidException ex) {        return new ResponseEntity&lt;&gt;(\"Invalid order data\", HttpStatus.BAD_REQUEST);    }}Conclusion An interface for the request body lets one endpoint accept many order types without a pile of branching at the door. We defined an Order interface, implemented it per type, let Jackson pick the right class from orderType, and validated with @Valid. Adding a new order type now means writing one class and one Jackson subtype, and nothing else has to change. Happy coding! ","categories": ["development"],
        "tags": ["java"],
        "url": "https://whyjun.github.io/blog/Using-Interface-for-Controller-Requests-in-Java%20Spring-Framework"
      },{
        "title": "Storing Heterogeneous Objects in PostgreSQL",
        "excerpt":"Storing different types of objects in one PostgreSQL table is tricky. Each type shares a few fields and carries its own. There are three practical ways to handle it, and each one trades flexibility for structure in a different spot. Scenario: Restaurant Order System Imagine a transactions table that stores different types of orders. Each order type has some common fields and some unique ones. Using JSONB for Orders The JSONB type is flexible. It lets you store different order types in one column without committing to a fixed shape. Schema CREATE TABLE transactions (    id SERIAL PRIMARY KEY,    orders JSONB);Inserting Data You insert data by writing the orders as JSON. INSERT INTO transactions (orders) VALUES (    '[        {\"type\": \"HamburgerOrder\", \"field1\": \"value1\", \"field2\": \"value2\"},        {\"type\": \"PizzaOrder\", \"field1\": \"value1\", \"field2\": \"value2\", \"uniqueField\": \"uniqueValue\"}    ]'::jsonb);Querying Data Retrieving specific orders stays simple. SELECT * FROM transactionsWHERE orders @&gt; '[{\"type\": \"HamburgerOrder\"}]';Pros and Cons   Pros: Very flexible. Simple schema.  Cons: Complex queries. Limited data validation.Using a Normalized Schema This method splits the data across several tables joined by foreign keys. It keeps the structure explicit and efficient. Schema Transactions Table: CREATE TABLE transactions (    id SERIAL PRIMARY KEY);Orders Table: CREATE TABLE orders (    id SERIAL PRIMARY KEY,    transaction_id INT REFERENCES transactions(id),    type VARCHAR(50) NOT NULL,    common_field1 TEXT,    common_field2 TEXT);HamburgerOrder Table: CREATE TABLE hamburger_orders (    id SERIAL PRIMARY KEY,    order_id INT REFERENCES orders(id),    unique_hamburger_field TEXT);PizzaOrder Table: CREATE TABLE pizza_orders (    id SERIAL PRIMARY KEY,    order_id INT REFERENCES orders(id),    unique_pizza_field TEXT);Inserting Data Insert the rows step by step. INSERT INTO transactions DEFAULT VALUES RETURNING id;INSERT INTO orders (transaction_id, type, common_field1, common_field2) VALUES(1, 'HamburgerOrder', 'value1', 'value2') RETURNING id;INSERT INTO hamburger_orders (order_id, unique_hamburger_field) VALUES(1, 'hamburgerValue');Querying Data Use JOIN queries to pull the orders back together. SELECT o.id AS order_id, o.type, o.common_field1, o.common_field2,       h.unique_hamburger_field, p.unique_pizza_fieldFROM orders oLEFT JOIN hamburger_orders h ON o.id = h.order_idLEFT JOIN pizza_orders p ON o.id = p.order_idWHERE o.transaction_id = 1;Pros and Cons   Pros: Strong data integrity. Efficient querying.  Cons: More complex schema. Requires more tables.Hybrid Approach: Common Fields + JSONB This approach mixes both. Common fields live in their own columns. Unique fields go into a JSONB column. Schema CREATE TABLE orders (    id SERIAL PRIMARY KEY,    transaction_id INT REFERENCES transactions(id),    type VARCHAR(50) NOT NULL,    common_field1 TEXT,    common_field2 TEXT,    specific_fields JSONB);Inserting Data Insert the common fields as columns and the rest as JSON. INSERT INTO orders (transaction_id, type, common_field1, common_field2, specific_fields) VALUES(1, 'HamburgerOrder', 'value1', 'value2', '{\"unique_hamburger_field\": \"hamburgerValue\"}'::jsonb),(1, 'PizzaOrder', 'value1', 'value2', '{\"unique_pizza_field\": \"pizzaValue\"}'::jsonb);Querying Data Query the common fields straight from the columns. SELECT * FROM orders WHERE transaction_id = 1;Pros and Cons   Pros: Flexibility with JSONB. Easier querying for common fields.  Cons: Validation can be tricky. Slightly more complex.Conclusion The right method depends on how strict your data has to be. JSONB is flexible but hard to validate. A normalized schema protects integrity but adds tables and joins. The hybrid keeps common fields queryable while leaving room for the odd ones. Pick the one that matches the shape of your data. Happy coding! ","categories": ["development"],
        "tags": ["java","sql"],
        "url": "https://whyjun.github.io/blog/Storing-Heterogeneous-Objects-in-PostgreSQL"
      },{
        "title": "How to Use Git Worktrees with Claude Code",
        "excerpt":"When Claude Code runs a long task, it edits files in your working directory. If you want to keep working while it does, you end up fighting over the same files. Git worktrees solve this. They let one repository check out several branches into separate folders at once, so Claude can work in one folder while you stay in another. What Is a Git Worktree A worktree is an extra working directory attached to your existing repository. Each worktree sits on its own branch and has its own files on disk, but they all share one .git history. You are not cloning the repo again, so you do not pay for a second copy of the object database. For Claude Code this means isolation. Point Claude at a worktree on a feature branch, and its edits, builds, and test runs stay out of your main checkout. Creating a Worktree Use git worktree add. Pass a path for the new folder and the branch to check out. git worktree add ../myproject-feature feature-branchThis creates myproject-feature next to your repo, checked out to feature-branch. If the branch does not exist yet, create it in the same command with -b. git worktree add -b new-feature ../myproject-featureYou now have two folders. Your original one stays where it was. The new one is on new-feature. Running Claude Code in the Worktree Open a terminal in the worktree folder and start Claude there. cd ../myproject-featureclaudeClaude now reads and writes files inside myproject-feature only. You can go back to your main folder and keep editing, run the app, or start a second Claude session on a different branch. The two sessions never touch the same files. Claude Code can also manage this for you. When you ask a subagent to work in isolation, it can create a temporary worktree, do its work there, and clean it up when the branch has no changes left. Listing and Removing Worktrees Check what you have with git worktree list. git worktree listWhen you are done with a worktree, remove it. This deletes the folder and unregisters it from the repo. git worktree remove ../myproject-featureIf you deleted the folder by hand, run git worktree prune to clear the stale entry. When Worktrees Help   Reviewing Claude’s changes on a branch while you keep working on another.  Running two Claude sessions in parallel on separate features.  Testing a build without stashing or committing half-finished work.When They Get in the Way   Small changes where a single branch switch would do.  Tooling that hardcodes an absolute path and breaks in a second folder.  Node or build caches that you now have to install once per worktree.Conclusion Git worktrees give Claude Code a folder of its own without a second clone. Create one with git worktree add, run Claude inside it, and remove it with git worktree remove when the branch is merged. If you often want Claude to work while you keep going, keep a spare worktree around and hand it the branch. Happy coding! ","categories": ["development"],
        "tags": ["claude-code","git"],
        "url": "https://whyjun.github.io/blog/How-to-Use-Git-Worktrees-with-Claude-Code"
      },{
        "title": "The Best AI Transformation Strategy Is Sharing Skills",
        "excerpt":"Most AI adoption plans start with tools and seats. Buy the licenses, run a workshop, tell everyone to use the agent. Adoption climbs for a week, then flattens. Everyone has the same model, and yet the results vary wildly from one person to the next. I used to think the gap was skill with prompts. Some people just knew how to talk to the agent. But that framing put the burden on each person to figure it out alone, over and over. What actually moved my team was smaller and easier to pass around: a shared skill. The Usual Playbook, and Why It Plateaus The default AI rollout treats the agent like a new IDE. Hand out access, point people at the docs, hope productivity follows. It rarely does. The agent is only as good as the context and instructions you give it. A raw agent with no guidance behaves like a very capable intern on their first day, every single day. It forgets what you told it last time. It guesses at your conventions. So each person quietly reinvents the same wheel. One teammate figures out how to get clean database migrations out of the agent. Another works out a solid review checklist. None of it leaves their machine. The org bought a hundred agents and got a hundred separate learning curves. What a Skill Actually Is A skill is a packaged, reusable capability for an agent: instructions, a bit of process, sometimes a script or a checklist, bundled so the agent can load it when it needs it. The format differs across tools, but the idea travels. You take something you figured out how to do well and write it down in a form the agent can execute, not just read. That last part is what matters. A wiki page tells a person what to do. A skill tells the agent what to do, and the agent is the one doing the work now. What Changed When I Shared One I had a skill I’d been using on my own for a routine task. It captured the steps I kept repeating, the mistakes I’d learned to avoid, and the shape of output I wanted. On my machine it was just a convenience. Then I shared it with my team. Nobody had to read a new document. Their agents just got better at the task the next day. A teammate who had never done it could invoke the skill and get output close to mine, because the hard-won judgment lived in the skill instead of in my head. My experience became their agent’s default behavior. It didn’t stop there. Someone hit a case my skill handled badly, fixed it, and pushed the fix back. The skill ended up better than the version I wrote. That surprised me. Once you share a skill it stops being your private tool, and the whole team starts sharpening it together. Why This Compounds Handing out agent licenses adds capacity in a straight line. Ten seats, ten people working a bit faster. Sharing skills compounds instead, because skills stack and circulate.   Every skill one person writes raises the floor for everyone, not just the author.  Skills improve as more people use them and feed edge cases back.  A good skill encodes taste and standards, so your team’s way of working spreads without a single meeting.  New teammates inherit the team’s collected judgment on day one instead of month six.Most orgs frame the goal as “our engineers use AI.” The goal worth aiming for is “our engineers’ agents keep getting better every week without anyone spending extra time on it.” Shared skills are how you get there. How to Start You don’t need a program for this. You need one skill and a place to put it.   Find the task you keep explaining to your agent the same way. That repetition is the signal.  Write it down as a skill, in whatever format your tooling supports.  Put it somewhere the team can pull from and edit, the way you already share code.  Invite people to change it. The first fix from someone else is the moment it stops being yours and becomes infrastructure.Do that once and you’ll feel the difference. Make it a habit and the agents around you keep getting sharper on their own. Conclusion The best AI transformation isn’t more seats or a better model. Everyone already has a capable agent. The leverage is in what you teach it, and in whether that teaching stays trapped on one laptop or spreads across the team. Share your skills. Let people sharpen them. Your team’s collective judgment turns into something every agent can run, and that advantage compounds. Happy building! ","categories": ["development"],
        "tags": ["ai","agents"],
        "url": "https://whyjun.github.io/blog/The-Best-AI-Transformation-Strategy-Is-Sharing-Skills"
      },{
        "title": "The 2026 Way to Write a Set in Go",
        "excerpt":"Back in 2021 I wrote a Korean post about building a Set in Go. I wasn’t arguing that Go needed one built in, I was solving algorithm problems, Advent of Code mostly, and kept running into spots where a set was the natural tool for the job. So I built one. Go is now at 1.26.4, the standard library has picked up generics, iterators, and a maps package along the way, and there’s still no Set type in sight (a proposal for one has been open for years without landing). That’s fine. The same trick from 2021 still solves the problem in 2026, it just looks a lot better written today. The Trick Hasn’t Changed A Set is a map where you only care about the keys. Go’s map already gives you O(1) inserts, lookups, and deletes with no duplicate keys, which is exactly what a set needs. The empty struct{} value costs zero bytes, so you’re not paying for values you’ll never read. type Set[T comparable] map[T]struct{}That’s the whole idea. What changed since 2021 is everything around it. From interface{} to Real Generics The 2021 version had to write map[interface{}]struct{}, because Go had no generics yet. Every value going in or out needed a type assertion, and the compiler couldn’t stop you from mixing an int and a string in the same set. With generics, Set[T comparable] gives you a type parameter instead. comparable is the built-in constraint for any type Go can use as a map key, so the set stays type-safe without you writing a single assertion. func NewSet[T comparable](items ...T) Set[T] {\ts := make(Set[T], len(items))\tfor _, item := range items {\t\ts.Add(item)\t}\treturn s}func (s Set[T]) Add(v T) {\ts[v] = struct{}{}}func (s Set[T]) Remove(v T) {\tdelete(s, v)}func (s Set[T]) Contains(v T) bool {\t_, ok := s[v]\treturn ok}func (s Set[T]) Len() int {\treturn len(s)}colors := NewSet(\"red\", \"green\", \"blue\")colors.Add(\"green\") // already there, no-opcolors.Remove(\"red\")colors.Contains(\"green\") // truecolors.Contains(\"red\")   // falsecolors.Len()              // 2Try passing an int into colors.Add and the compiler rejects it before the program ever runs. That’s the whole win over the interface{} version: the same map-of-empty-struct trick, minus the runtime type checks. Iterating Without the Boilerplate The 2021 post didn’t cover iteration, because looping over a map[interface{}]struct{} and asserting each key back to its real type wasn’t worth showing. Since Go 1.23, the maps package exposes maps.Keys, which returns an iter.Seq[T] you can range over directly. import \"maps\"for c := range maps.Keys(colors) {\tfmt.Println(\"color:\", c)}If you don’t want callers importing maps just to walk your set, give the type its own iterator method and return the same thing: import \"iter\"func (s Set[T]) All() iter.Seq[T] {\treturn maps.Keys(s)}for v := range colors.All() {\tfmt.Println(v)}Set Algebra for Free Generics also make union and intersection easy to write once, for every element type, instead of copy-pasting a version per type: func Union[T comparable](a, b Set[T]) Set[T] {\tresult := make(Set[T], len(a)+len(b))\tfor v := range a {\t\tresult.Add(v)\t}\tfor v := range b {\t\tresult.Add(v)\t}\treturn result}func Intersect[T comparable](a, b Set[T]) Set[T] {\tresult := Set[T]{}\tfor v := range a {\t\tif b.Contains(v) {\t\t\tresult.Add(v)\t\t}\t}\treturn result}a := NewSet(1, 2, 3)b := NewSet(3, 4, 5)Union(a, b).Len()     // 5Intersect(a, b).Len() // 1I ran every snippet above against Go 1.26.4, the latest stable release as of this writing, and checked them with go vet and gofmt before publishing. Conclusion If you hit the same wall I hit in 2021, solving algorithm problems and reaching for a Set that isn’t there, the fix is still to build one, and Go still hands you good pieces to do it with: a map, an empty struct{}, and now comparable generics and the maps package to strip out the boilerplate around them. The core idea aged fine. Everything wrapped around it got sharper. Happy coding! ","categories": ["Golang"],
        "tags": ["golang","generics","data structure"],
        "url": "https://whyjun.github.io/golang/The-2026-Way-to-Write-a-Set-in-Go"
      }]
