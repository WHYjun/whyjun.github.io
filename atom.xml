<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://whyjun.github.io/atom.xml" rel="self" type="application/atom+xml" /><link href="https://whyjun.github.io/" rel="alternate" type="text/html" /><updated>2026-07-08T08:29:46-07:00</updated><id>https://whyjun.github.io/atom.xml</id><title type="html">Young’s Notes</title><subtitle>Notes from my software engineering journey: what I learn, what I try, and how I set things up.</subtitle><author><name>Young Choi</name></author><entry xml:lang="en"><title type="html">The 2026 Way to Lint Go Code with golangci-lint</title><link href="https://whyjun.github.io/golang/The-2026-Way-to-Lint-Go-Code-with-golangci-lint" rel="alternate" type="text/html" title="The 2026 Way to Lint Go Code with golangci-lint" /><published>2026-07-08T00:00:00-07:00</published><updated>2026-07-08T00:00:00-07:00</updated><id>https://whyjun.github.io/golang/The-2026-Way-to-Lint-Go-Code-with-golangci-lint</id><content type="html" xml:base="https://whyjun.github.io/golang/The-2026-Way-to-Lint-Go-Code-with-golangci-lint"><![CDATA[<p>Back in 2023 I wrote a Korean post about adopting <code class="language-plaintext highlighter-rouge">golangci-lint</code> on my team. We’d grown enough that reviewers were spending more time explaining idiomatic Go in PR comments than reviewing the actual logic, and we needed something automated to take that job off our plates. <code class="language-plaintext highlighter-rouge">golangci-lint</code> was the fix: instead of running <code class="language-plaintext highlighter-rouge">govet</code>, <code class="language-plaintext highlighter-rouge">gofmt</code>, and a dozen other tools one at a time, it runs all of them in parallel and caches the results. Three years later that’s still exactly why I reach for it. What’s changed is everything around it, most of all the config file, which went through a major version bump in 2025.</p>

<h2 id="why-golangci-lint-still">Why golangci-lint, Still</h2>

<p>The pitch hasn’t moved. Go’s ecosystem has plenty of good standalone linters, but running each one separately is slow and means juggling a dozen install commands and flag sets. <code class="language-plaintext highlighter-rouge">golangci-lint</code> wraps them behind one binary, runs them concurrently, and caches analysis results between runs so repeat lints are fast. That’s still the whole value proposition, and it’s still worth it the moment more than one person touches a repo.</p>

<h2 id="installing-it">Installing It</h2>

<p>I installed <code class="language-plaintext highlighter-rouge">golangci-lint</code> fresh today to write this post, and the install story has tightened up. The docs now actively discourage <code class="language-plaintext highlighter-rouge">go install</code>, calling it “not guaranteed to work” for this project, and point everyone at the binary installer instead. On Windows, that means Git Bash:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-sSfL</span> https://golangci-lint.run/install.sh | sh <span class="nt">-s</span> <span class="nt">--</span> <span class="nt">-b</span> <span class="si">$(</span>go <span class="nb">env </span>GOPATH<span class="si">)</span>/bin v2.12.2

golangci-lint <span class="nt">--version</span>
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>golangci-lint has version 2.12.2 built with go1.26.2 from c0d3ddc9 on 2026-05-06T11:07:58Z
</code></pre></div></div>

<p>That’s the current version as of this post. Pin a version in the install command instead of trusting <code class="language-plaintext highlighter-rouge">latest</code>, the same way you’d pin any other build tool.</p>

<h2 id="the-config-file-got-a-major-version-bump">The Config File Got a Major Version Bump</h2>

<p>This is the part of the 2023 post that’s fully out of date. <code class="language-plaintext highlighter-rouge">golangci-lint</code> v2.0.0 shipped in March 2025 with a rewritten config schema, and every <code class="language-plaintext highlighter-rouge">.golangci.yml</code> I’d written before that no longer loads as-is. The two changes that matter:</p>

<ul>
  <li>The file needs a top-level <code class="language-plaintext highlighter-rouge">version: "2"</code>.</li>
  <li>Linters and formatters are now separate sections. Tools like <code class="language-plaintext highlighter-rouge">gofmt</code>, <code class="language-plaintext highlighter-rouge">gofumpt</code>, <code class="language-plaintext highlighter-rouge">goimports</code>, and <code class="language-plaintext highlighter-rouge">gci</code> used to live under <code class="language-plaintext highlighter-rouge">linters</code>; they moved to their own <code class="language-plaintext highlighter-rouge">formatters</code> block, because formatting code and flagging bugs in it are different jobs.</li>
</ul>

<p>Here’s a minimal config in the new shape, which I ran against a real Go file to confirm it loads and works:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">2"</span>
<span class="na">linters</span><span class="pi">:</span>
  <span class="na">default</span><span class="pi">:</span> <span class="s">standard</span>
  <span class="na">enable</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">errorlint</span>
<span class="na">formatters</span><span class="pi">:</span>
  <span class="na">enable</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">gofmt</span>
    <span class="pi">-</span> <span class="s">goimports</span>
</code></pre></div></div>

<p>If you’ve still got a v1 config lying around, don’t hand-migrate it. <code class="language-plaintext highlighter-rouge">golangci-lint migrate</code> does it for you:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>golangci-lint migrate <span class="nt">-c</span> .golangci.yml
</code></pre></div></div>

<p>I fed it an old-style config with an <code class="language-plaintext highlighter-rouge">errorlint</code> and <code class="language-plaintext highlighter-rouge">gofmt</code> entry under <code class="language-plaintext highlighter-rouge">linters</code>, and it rewrote the file in place: added <code class="language-plaintext highlighter-rouge">version: "2"</code>, moved <code class="language-plaintext highlighter-rouge">gofmt</code> into <code class="language-plaintext highlighter-rouge">formatters</code>, and warned me that <code class="language-plaintext highlighter-rouge">run.timeout</code> no longer applies because v2 disables the timeout by default. It even flagged that comments in the original file don’t survive the migration, so it’s worth a manual diff afterward rather than trusting it blindly.</p>

<h2 id="running-it">Running It</h2>

<p><code class="language-plaintext highlighter-rouge">golangci-lint run</code> still works exactly like before, and the linters it finds are still worth having. I wrote a small function with the exact bug my 2023 post talked about, wrapping an error with <code class="language-plaintext highlighter-rouge">%s</code> instead of <code class="language-plaintext highlighter-rouge">%w</code>:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">readConfig</span><span class="p">(</span><span class="n">path</span> <span class="kt">string</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
	<span class="n">_</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">os</span><span class="o">.</span><span class="n">ReadFile</span><span class="p">(</span><span class="n">path</span><span class="p">)</span>
	<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"reading config: %s"</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="no">nil</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>golangci-lint run ./...
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>main.go:11:43: non-wrapping format verb for fmt.Errorf. Use `%w` to format errors (errorlint)
		return fmt.Errorf("reading config: %s", err)
		                                        ^
1 issues:
* errorlint: 1
</code></pre></div></div>

<p>Same catch as three years ago: <code class="language-plaintext highlighter-rouge">%s</code> loses the wrapped error, so <code class="language-plaintext highlighter-rouge">errors.Is</code> and <code class="language-plaintext highlighter-rouge">errors.As</code> can’t see through it later, which matters more the bigger the codebase gets.</p>

<p>One flag did change. The old <code class="language-plaintext highlighter-rouge">--fast</code> flag is gone; running it now just fails with an unknown-flag error. Its replacement is <code class="language-plaintext highlighter-rouge">--fast-only</code>, which filters the enabled set down to linters tagged fast:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>golangci-lint run <span class="nt">--fast-only</span> ./...
</code></pre></div></div>

<p>On my test file that came back with 0 issues, because <code class="language-plaintext highlighter-rouge">errorlint</code> isn’t tagged as a fast linter and got filtered out, exactly the trade-off you’re opting into when you reach for <code class="language-plaintext highlighter-rouge">--fast-only</code> for a quick pre-commit check instead of the full run.</p>

<h2 id="visual-studio-code">Visual Studio Code</h2>

<p>The <code class="language-plaintext highlighter-rouge">go.lintTool</code> and <code class="language-plaintext highlighter-rouge">go.lintFlags</code> settings I wrote about in 2023 are still there in the Go extension, but <code class="language-plaintext highlighter-rouge">go.lintTool</code> now has an extra option. <code class="language-plaintext highlighter-rouge">golangci-lint</code> means v1, and there’s a separate <code class="language-plaintext highlighter-rouge">golangci-lint-v2</code> value for the current major version:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">"go.lintTool"</span><span class="p">:</span><span class="w"> </span><span class="s2">"golangci-lint-v2"</span><span class="err">,</span><span class="w">
</span><span class="nl">"go.lintFlags"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"--fast-only"</span><span class="p">]</span><span class="w">
</span></code></pre></div></div>

<p>If you’re still on the plain <code class="language-plaintext highlighter-rouge">golangci-lint</code> setting and a v2-only config, the extension will be reading a config it doesn’t understand, so check which value you have set before you assume it’s working.</p>

<h2 id="conclusion">Conclusion</h2>

<p>The reason to use <code class="language-plaintext highlighter-rouge">golangci-lint</code> hasn’t changed since 2023: one tool, run in parallel, caching results, instead of a dozen linters bolted together by hand. What changed is the config file underneath it, and it changed enough that a three-year-old <code class="language-plaintext highlighter-rouge">.golangci.yml</code> won’t load until you either rewrite it by hand or let <code class="language-plaintext highlighter-rouge">golangci-lint migrate</code> do it. Run that once, fix your CI flags if you were relying on <code class="language-plaintext highlighter-rouge">--fast</code>, and the rest of the setup carries over untouched. Happy coding!</p>]]></content><author><name>Young Choi</name></author><category term="Golang" /><category term="golang" /><category term="tooling" /><category term="lint" /><summary type="html"><![CDATA[Back in 2023 I wrote a Korean post about adopting golangci-lint on my team. We’d grown enough that reviewers were spending more time explaining idiomatic Go in PR comments than reviewing the actual logic, and we needed something automated to take that job off our plates. golangci-lint was the fix: instead of running govet, gofmt, and a dozen other tools one at a time, it runs all of them in parallel and caches the results. Three years later that’s still exactly why I reach for it. What’s changed is everything around it, most of all the config file, which went through a major version bump in 2025.]]></summary></entry><entry xml:lang="en"><title type="html">The 2026 Way to Write Enums in Go</title><link href="https://whyjun.github.io/golang/The-2026-Way-to-Write-Enums-in-Go" rel="alternate" type="text/html" title="The 2026 Way to Write Enums in Go" /><published>2026-07-06T00:00:00-07:00</published><updated>2026-07-06T00:00:00-07:00</updated><id>https://whyjun.github.io/golang/The-2026-Way-to-Write-Enums-in-Go</id><content type="html" xml:base="https://whyjun.github.io/golang/The-2026-Way-to-Write-Enums-in-Go"><![CDATA[<p>Back in 2022 I wrote a Korean post about faking enums in Go with a typed constant block and <code class="language-plaintext highlighter-rouge">iota</code>. Go still has no <code class="language-plaintext highlighter-rouge">enum</code> keyword in 1.26.4, and a couple of language proposals asking for one (<a href="https://github.com/golang/go/issues/19814">issue #19814</a>, <a href="https://github.com/golang/go/issues/64739">issue #64739</a>) have been open for years without landing. So the trick from 2022 is still exactly how you write an enum in Go today. What’s worth a second post is the bug hiding in that trick, because this time I actually ran it instead of just describing it.</p>

<h2 id="the-trick-hasnt-changed">The Trick Hasn’t Changed</h2>

<p>Go has no <code class="language-plaintext highlighter-rouge">enum</code> type, so you fake one with a named integer type and a <code class="language-plaintext highlighter-rouge">const</code> block that uses <code class="language-plaintext highlighter-rouge">iota</code> to number the values:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">type</span> <span class="n">Status</span> <span class="kt">int</span>

<span class="k">const</span> <span class="p">(</span>
	<span class="n">StatusOK</span> <span class="n">Status</span> <span class="o">=</span> <span class="no">iota</span>
	<span class="n">StatusError</span>
	<span class="n">StatusPending</span>
<span class="p">)</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">iota</code> starts at 0 and increments by one per line, so <code class="language-plaintext highlighter-rouge">StatusOK</code> is <code class="language-plaintext highlighter-rouge">0</code>, <code class="language-plaintext highlighter-rouge">StatusError</code> is <code class="language-plaintext highlighter-rouge">1</code>, and <code class="language-plaintext highlighter-rouge">StatusPending</code> is <code class="language-plaintext highlighter-rouge">2</code>. It reads cleanly and the compiler stops you from mixing a <code class="language-plaintext highlighter-rouge">Status</code> with a plain <code class="language-plaintext highlighter-rouge">int</code> by accident.</p>

<h2 id="where-it-bites-you">Where It Bites You</h2>

<p><code class="language-plaintext highlighter-rouge">iota</code> always starts at 0, and 0 is also Go’s zero value for <code class="language-plaintext highlighter-rouge">int</code>. If you never explicitly set a <code class="language-plaintext highlighter-rouge">Status</code> field, decoding a struct from JSON, reading an unset field from a database row, or just declaring <code class="language-plaintext highlighter-rouge">var s Status</code> all hand you <code class="language-plaintext highlighter-rouge">0</code> for free. That’s fine as long as <code class="language-plaintext highlighter-rouge">0</code> doesn’t mean something you’d act on.</p>

<p>In the example above, it does. Here’s a <code class="language-plaintext highlighter-rouge">Request</code> struct with a <code class="language-plaintext highlighter-rouge">Status</code> field, and a JSON payload from a queue where whoever built it forgot to set the status:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">type</span> <span class="n">Request</span> <span class="k">struct</span> <span class="p">{</span>
	<span class="n">ID</span>        <span class="kt">int</span>    <span class="s">`json:"ID"`</span>
	<span class="n">Timestamp</span> <span class="kt">int</span>    <span class="s">`json:"Timestamp"`</span>
	<span class="n">Message</span>   <span class="kt">string</span> <span class="s">`json:"Message"`</span>
	<span class="n">Status</span>    <span class="n">Status</span> <span class="s">`json:"Status"`</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"ID"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w">
  </span><span class="nl">"Timestamp"</span><span class="p">:</span><span class="w"> </span><span class="mi">1738000000</span><span class="p">,</span><span class="w">
  </span><span class="nl">"Message"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Example message"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>I unmarshaled that payload against the <code class="language-plaintext highlighter-rouge">Request</code> struct and printed what came out:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">var</span> <span class="n">req</span> <span class="n">Request</span>
<span class="n">json</span><span class="o">.</span><span class="n">Unmarshal</span><span class="p">(</span><span class="n">payload</span><span class="p">,</span> <span class="o">&amp;</span><span class="n">req</span><span class="p">)</span>
<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="n">req</span><span class="o">.</span><span class="n">Status</span><span class="p">)</span> <span class="c">// 0</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">req.Status</code> decodes as <code class="language-plaintext highlighter-rouge">0</code>, which is <code class="language-plaintext highlighter-rouge">StatusOK</code>. A request that never had its status set now looks like a request that succeeded. Nothing errors, nothing panics, the bug just quietly ships. I ran this end to end rather than assuming it, and the zero-value collision is exactly as silent as it sounds: <code class="language-plaintext highlighter-rouge">go vet</code> has nothing to say about it, because <code class="language-plaintext highlighter-rouge">0</code> is a perfectly valid <code class="language-plaintext highlighter-rouge">Status</code>.</p>

<h2 id="the-fix">The Fix</h2>

<p>Reserve <code class="language-plaintext highlighter-rouge">0</code> for a case that means “nothing was set,” and start your real values at 1:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">type</span> <span class="n">Status</span> <span class="kt">int</span>

<span class="k">const</span> <span class="p">(</span>
	<span class="n">StatusUnknown</span> <span class="n">Status</span> <span class="o">=</span> <span class="no">iota</span>
	<span class="n">StatusOK</span>
	<span class="n">StatusError</span>
	<span class="n">StatusPending</span>
<span class="p">)</span>
</code></pre></div></div>

<p>Now the same missing-field payload decodes to <code class="language-plaintext highlighter-rouge">StatusUnknown</code> instead of <code class="language-plaintext highlighter-rouge">StatusOK</code>. A forgotten field turns into a visible, checkable state instead of a false positive for success. I reran the exact same unmarshal against this version and got <code class="language-plaintext highlighter-rouge">StatusUnknown</code> back, not <code class="language-plaintext highlighter-rouge">StatusOK</code>.</p>

<p>This costs you nothing. You still get sequential values, you still get type safety, and now the zero value tells you “this was never set” instead of lying to you.</p>

<h2 id="catching-it-automatically">Catching It Automatically</h2>

<p>If you’d rather not rely on remembering this every time you define an enum, the <a href="https://github.com/nishanths/exhaustive"><code class="language-plaintext highlighter-rouge">exhaustive</code></a> linter (bundled with <code class="language-plaintext highlighter-rouge">golangci-lint</code>) flags <code class="language-plaintext highlighter-rouge">switch</code> statements over an enum type that don’t cover every constant. It won’t catch the zero-value trap directly, but it does force you to handle every <code class="language-plaintext highlighter-rouge">Status</code> value explicitly wherever you branch on one, which tends to surface an unhandled <code class="language-plaintext highlighter-rouge">StatusUnknown</code> case as soon as you add it.</p>

<h2 id="conclusion">Conclusion</h2>

<p>The core trick for an enum in Go hasn’t moved since 2022: a named integer type plus a <code class="language-plaintext highlighter-rouge">const</code>/<code class="language-plaintext highlighter-rouge">iota</code> block. What’s worth remembering is that <code class="language-plaintext highlighter-rouge">iota</code> starts at 0, and 0 is also your zero value, so your first enum constant is what every unset, forgotten, or zeroed-out <code class="language-plaintext highlighter-rouge">Status</code> silently becomes. Give that slot to an explicit <code class="language-plaintext highlighter-rouge">Unknown</code> case and the zero value stops lying to you. Happy coding!</p>]]></content><author><name>Young Choi</name></author><category term="Golang" /><category term="golang" /><category term="enums" /><summary type="html"><![CDATA[Back in 2022 I wrote a Korean post about faking enums in Go with a typed constant block and iota. Go still has no enum keyword in 1.26.4, and a couple of language proposals asking for one (issue #19814, issue #64739) have been open for years without landing. So the trick from 2022 is still exactly how you write an enum in Go today. What’s worth a second post is the bug hiding in that trick, because this time I actually ran it instead of just describing it.]]></summary></entry><entry xml:lang="en"><title type="html">The 2026 Way to Write a Set in Go</title><link href="https://whyjun.github.io/golang/The-2026-Way-to-Write-a-Set-in-Go" rel="alternate" type="text/html" title="The 2026 Way to Write a Set in Go" /><published>2026-07-05T00:00:00-07:00</published><updated>2026-07-05T00:00:00-07:00</updated><id>https://whyjun.github.io/golang/The-2026-Way-to-Write-a-Set-in-Go</id><content type="html" xml:base="https://whyjun.github.io/golang/The-2026-Way-to-Write-a-Set-in-Go"><![CDATA[<p>Back in 2021 I wrote a Korean post about building a <code class="language-plaintext highlighter-rouge">Set</code> in Go. I wasn’t arguing that Go needed one built in, I was solving algorithm problems, Advent of Code mostly, and kept running into spots where a set was the natural tool for the job. So I built one. Go is now at 1.26.4, the standard library has picked up generics, iterators, and a <code class="language-plaintext highlighter-rouge">maps</code> package along the way, and there’s still no <code class="language-plaintext highlighter-rouge">Set</code> type in sight (a <a href="https://github.com/golang/go/issues/69230">proposal for one</a> has been open for years without landing). That’s fine. The same trick from 2021 still solves the problem in 2026, it just looks a lot better written today.</p>

<h2 id="the-trick-hasnt-changed">The Trick Hasn’t Changed</h2>

<p>A <code class="language-plaintext highlighter-rouge">Set</code> is a <code class="language-plaintext highlighter-rouge">map</code> where you only care about the keys. Go’s <code class="language-plaintext highlighter-rouge">map</code> already gives you O(1) inserts, lookups, and deletes with no duplicate keys, which is exactly what a set needs. The empty <code class="language-plaintext highlighter-rouge">struct{}</code> value costs zero bytes, so you’re not paying for values you’ll never read.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">type</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span> <span class="n">comparable</span><span class="p">]</span> <span class="k">map</span><span class="p">[</span><span class="n">T</span><span class="p">]</span><span class="k">struct</span><span class="p">{}</span>
</code></pre></div></div>

<p>That’s the whole idea. What changed since 2021 is everything around it.</p>

<h2 id="from-interface-to-real-generics">From <code class="language-plaintext highlighter-rouge">interface{}</code> to Real Generics</h2>

<p>The 2021 version had to write <code class="language-plaintext highlighter-rouge">map[interface{}]struct{}</code>, because Go had no generics yet. Every value going in or out needed a type assertion, and the compiler couldn’t stop you from mixing an <code class="language-plaintext highlighter-rouge">int</code> and a <code class="language-plaintext highlighter-rouge">string</code> in the same set.</p>

<p>With generics, <code class="language-plaintext highlighter-rouge">Set[T comparable]</code> gives you a type parameter instead. <code class="language-plaintext highlighter-rouge">comparable</code> is the built-in constraint for any type Go can use as a map key, so the set stays type-safe without you writing a single assertion.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">NewSet</span><span class="p">[</span><span class="n">T</span> <span class="n">comparable</span><span class="p">](</span><span class="n">items</span> <span class="o">...</span><span class="n">T</span><span class="p">)</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">]</span> <span class="p">{</span>
	<span class="n">s</span> <span class="o">:=</span> <span class="nb">make</span><span class="p">(</span><span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">],</span> <span class="nb">len</span><span class="p">(</span><span class="n">items</span><span class="p">))</span>
	<span class="k">for</span> <span class="n">_</span><span class="p">,</span> <span class="n">item</span> <span class="o">:=</span> <span class="k">range</span> <span class="n">items</span> <span class="p">{</span>
		<span class="n">s</span><span class="o">.</span><span class="n">Add</span><span class="p">(</span><span class="n">item</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="n">s</span>
<span class="p">}</span>

<span class="k">func</span> <span class="p">(</span><span class="n">s</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">])</span> <span class="n">Add</span><span class="p">(</span><span class="n">v</span> <span class="n">T</span><span class="p">)</span> <span class="p">{</span>
	<span class="n">s</span><span class="p">[</span><span class="n">v</span><span class="p">]</span> <span class="o">=</span> <span class="k">struct</span><span class="p">{}{}</span>
<span class="p">}</span>

<span class="k">func</span> <span class="p">(</span><span class="n">s</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">])</span> <span class="n">Remove</span><span class="p">(</span><span class="n">v</span> <span class="n">T</span><span class="p">)</span> <span class="p">{</span>
	<span class="nb">delete</span><span class="p">(</span><span class="n">s</span><span class="p">,</span> <span class="n">v</span><span class="p">)</span>
<span class="p">}</span>

<span class="k">func</span> <span class="p">(</span><span class="n">s</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">])</span> <span class="n">Contains</span><span class="p">(</span><span class="n">v</span> <span class="n">T</span><span class="p">)</span> <span class="kt">bool</span> <span class="p">{</span>
	<span class="n">_</span><span class="p">,</span> <span class="n">ok</span> <span class="o">:=</span> <span class="n">s</span><span class="p">[</span><span class="n">v</span><span class="p">]</span>
	<span class="k">return</span> <span class="n">ok</span>
<span class="p">}</span>

<span class="k">func</span> <span class="p">(</span><span class="n">s</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">])</span> <span class="n">Len</span><span class="p">()</span> <span class="kt">int</span> <span class="p">{</span>
	<span class="k">return</span> <span class="nb">len</span><span class="p">(</span><span class="n">s</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">colors</span> <span class="o">:=</span> <span class="n">NewSet</span><span class="p">(</span><span class="s">"red"</span><span class="p">,</span> <span class="s">"green"</span><span class="p">,</span> <span class="s">"blue"</span><span class="p">)</span>
<span class="n">colors</span><span class="o">.</span><span class="n">Add</span><span class="p">(</span><span class="s">"green"</span><span class="p">)</span> <span class="c">// already there, no-op</span>
<span class="n">colors</span><span class="o">.</span><span class="n">Remove</span><span class="p">(</span><span class="s">"red"</span><span class="p">)</span>

<span class="n">colors</span><span class="o">.</span><span class="n">Contains</span><span class="p">(</span><span class="s">"green"</span><span class="p">)</span> <span class="c">// true</span>
<span class="n">colors</span><span class="o">.</span><span class="n">Contains</span><span class="p">(</span><span class="s">"red"</span><span class="p">)</span>   <span class="c">// false</span>
<span class="n">colors</span><span class="o">.</span><span class="n">Len</span><span class="p">()</span>              <span class="c">// 2</span>
</code></pre></div></div>

<p>Try passing an <code class="language-plaintext highlighter-rouge">int</code> into <code class="language-plaintext highlighter-rouge">colors.Add</code> and the compiler rejects it before the program ever runs. That’s the whole win over the <code class="language-plaintext highlighter-rouge">interface{}</code> version: the same map-of-empty-struct trick, minus the runtime type checks.</p>

<h2 id="iterating-without-the-boilerplate">Iterating Without the Boilerplate</h2>

<p>The 2021 post didn’t cover iteration, because looping over a <code class="language-plaintext highlighter-rouge">map[interface{}]struct{}</code> and asserting each key back to its real type wasn’t worth showing. Since Go 1.23, the <code class="language-plaintext highlighter-rouge">maps</code> package exposes <code class="language-plaintext highlighter-rouge">maps.Keys</code>, which returns an <code class="language-plaintext highlighter-rouge">iter.Seq[T]</code> you can range over directly.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="s">"maps"</span>

<span class="k">for</span> <span class="n">c</span> <span class="o">:=</span> <span class="k">range</span> <span class="n">maps</span><span class="o">.</span><span class="n">Keys</span><span class="p">(</span><span class="n">colors</span><span class="p">)</span> <span class="p">{</span>
	<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">"color:"</span><span class="p">,</span> <span class="n">c</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>If you don’t want callers importing <code class="language-plaintext highlighter-rouge">maps</code> just to walk your set, give the type its own iterator method and return the same thing:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="s">"iter"</span>

<span class="k">func</span> <span class="p">(</span><span class="n">s</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">])</span> <span class="n">All</span><span class="p">()</span> <span class="n">iter</span><span class="o">.</span><span class="n">Seq</span><span class="p">[</span><span class="n">T</span><span class="p">]</span> <span class="p">{</span>
	<span class="k">return</span> <span class="n">maps</span><span class="o">.</span><span class="n">Keys</span><span class="p">(</span><span class="n">s</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">v</span> <span class="o">:=</span> <span class="k">range</span> <span class="n">colors</span><span class="o">.</span><span class="n">All</span><span class="p">()</span> <span class="p">{</span>
	<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="n">v</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="set-algebra-for-free">Set Algebra for Free</h2>

<p>Generics also make union and intersection easy to write once, for every element type, instead of copy-pasting a version per type:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">Union</span><span class="p">[</span><span class="n">T</span> <span class="n">comparable</span><span class="p">](</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">])</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">]</span> <span class="p">{</span>
	<span class="n">result</span> <span class="o">:=</span> <span class="nb">make</span><span class="p">(</span><span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">],</span> <span class="nb">len</span><span class="p">(</span><span class="n">a</span><span class="p">)</span><span class="o">+</span><span class="nb">len</span><span class="p">(</span><span class="n">b</span><span class="p">))</span>
	<span class="k">for</span> <span class="n">v</span> <span class="o">:=</span> <span class="k">range</span> <span class="n">a</span> <span class="p">{</span>
		<span class="n">result</span><span class="o">.</span><span class="n">Add</span><span class="p">(</span><span class="n">v</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="k">for</span> <span class="n">v</span> <span class="o">:=</span> <span class="k">range</span> <span class="n">b</span> <span class="p">{</span>
		<span class="n">result</span><span class="o">.</span><span class="n">Add</span><span class="p">(</span><span class="n">v</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="n">result</span>
<span class="p">}</span>

<span class="k">func</span> <span class="n">Intersect</span><span class="p">[</span><span class="n">T</span> <span class="n">comparable</span><span class="p">](</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">])</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">]</span> <span class="p">{</span>
	<span class="n">result</span> <span class="o">:=</span> <span class="n">Set</span><span class="p">[</span><span class="n">T</span><span class="p">]{}</span>
	<span class="k">for</span> <span class="n">v</span> <span class="o">:=</span> <span class="k">range</span> <span class="n">a</span> <span class="p">{</span>
		<span class="k">if</span> <span class="n">b</span><span class="o">.</span><span class="n">Contains</span><span class="p">(</span><span class="n">v</span><span class="p">)</span> <span class="p">{</span>
			<span class="n">result</span><span class="o">.</span><span class="n">Add</span><span class="p">(</span><span class="n">v</span><span class="p">)</span>
		<span class="p">}</span>
	<span class="p">}</span>
	<span class="k">return</span> <span class="n">result</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">a</span> <span class="o">:=</span> <span class="n">NewSet</span><span class="p">(</span><span class="m">1</span><span class="p">,</span> <span class="m">2</span><span class="p">,</span> <span class="m">3</span><span class="p">)</span>
<span class="n">b</span> <span class="o">:=</span> <span class="n">NewSet</span><span class="p">(</span><span class="m">3</span><span class="p">,</span> <span class="m">4</span><span class="p">,</span> <span class="m">5</span><span class="p">)</span>

<span class="n">Union</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span><span class="o">.</span><span class="n">Len</span><span class="p">()</span>     <span class="c">// 5</span>
<span class="n">Intersect</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span><span class="o">.</span><span class="n">Len</span><span class="p">()</span> <span class="c">// 1</span>
</code></pre></div></div>

<p>I ran every snippet above against Go 1.26.4, the latest stable release as of this writing, and checked them with <code class="language-plaintext highlighter-rouge">go vet</code> and <code class="language-plaintext highlighter-rouge">gofmt</code> before publishing.</p>

<h2 id="conclusion">Conclusion</h2>

<p>If you hit the same wall I hit in 2021, solving algorithm problems and reaching for a <code class="language-plaintext highlighter-rouge">Set</code> that isn’t there, the fix is still to build one, and Go still hands you good pieces to do it with: a <code class="language-plaintext highlighter-rouge">map</code>, an empty <code class="language-plaintext highlighter-rouge">struct{}</code>, and now <code class="language-plaintext highlighter-rouge">comparable</code> generics and the <code class="language-plaintext highlighter-rouge">maps</code> package to strip out the boilerplate around them. The core idea aged fine. Everything wrapped around it got sharper. Happy coding!</p>]]></content><author><name>Young Choi</name></author><category term="Golang" /><category term="golang" /><category term="generics" /><category term="data structure" /><summary type="html"><![CDATA[Back in 2021 I wrote a Korean post about building a Set in Go. I wasn’t arguing that Go needed one built in, I was solving algorithm problems, Advent of Code mostly, and kept running into spots where a set was the natural tool for the job. So I built one. Go is now at 1.26.4, the standard library has picked up generics, iterators, and a maps package along the way, and there’s still no Set type in sight (a proposal for one has been open for years without landing). That’s fine. The same trick from 2021 still solves the problem in 2026, it just looks a lot better written today.]]></summary></entry><entry xml:lang="en"><title type="html">The Best AI Transformation Strategy Is Sharing Skills</title><link href="https://whyjun.github.io/blog/The-Best-AI-Transformation-Strategy-Is-Sharing-Skills" rel="alternate" type="text/html" title="The Best AI Transformation Strategy Is Sharing Skills" /><published>2026-07-04T00:00:00-07:00</published><updated>2026-07-04T00:00:00-07:00</updated><id>https://whyjun.github.io/blog/The-Best-AI-Transformation-Strategy-Is-Sharing-Skills</id><content type="html" xml:base="https://whyjun.github.io/blog/The-Best-AI-Transformation-Strategy-Is-Sharing-Skills"><![CDATA[<p>Most AI adoption plans start with tools and seats. Buy the licenses, run a workshop, tell everyone to use the agent. Adoption climbs for a week, then flattens. Everyone has the same model, and yet the results vary wildly from one person to the next.</p>

<p>I used to think the gap was skill with prompts. Some people just knew how to talk to the agent. But that framing put the burden on each person to figure it out alone, over and over. What actually moved my team was smaller and easier to pass around: a shared skill.</p>

<h2 id="the-usual-playbook-and-why-it-plateaus">The Usual Playbook, and Why It Plateaus</h2>

<p>The default AI rollout treats the agent like a new IDE. Hand out access, point people at the docs, hope productivity follows. It rarely does. The agent is only as good as the context and instructions you give it. A raw agent with no guidance behaves like a very capable intern on their first day, every single day. It forgets what you told it last time. It guesses at your conventions.</p>

<p>So each person quietly reinvents the same wheel. One teammate figures out how to get clean database migrations out of the agent. Another works out a solid review checklist. None of it leaves their machine. The org bought a hundred agents and got a hundred separate learning curves.</p>

<h2 id="what-a-skill-actually-is">What a Skill Actually Is</h2>

<p>A skill is a packaged, reusable capability for an agent: instructions, a bit of process, sometimes a script or a checklist, bundled so the agent can load it when it needs it. The format differs across tools, but the idea travels. You take something you figured out how to do well and write it down in a form the agent can execute, not just read.</p>

<p>That last part is what matters. A wiki page tells a person what to do. A skill tells the agent what to do, and the agent is the one doing the work now.</p>

<h2 id="what-changed-when-i-shared-one">What Changed When I Shared One</h2>

<p>I had a skill I’d been using on my own for a routine task. It captured the steps I kept repeating, the mistakes I’d learned to avoid, and the shape of output I wanted. On my machine it was just a convenience.</p>

<p>Then I shared it with my team.</p>

<p>Nobody had to read a new document. Their agents just got better at the task the next day. A teammate who had never done it could invoke the skill and get output close to mine, because the hard-won judgment lived in the skill instead of in my head. My experience became their agent’s default behavior.</p>

<p>It didn’t stop there. Someone hit a case my skill handled badly, fixed it, and pushed the fix back. The skill ended up better than the version I wrote. That surprised me. Once you share a skill it stops being your private tool, and the whole team starts sharpening it together.</p>

<h2 id="why-this-compounds">Why This Compounds</h2>

<p>Handing out agent licenses adds capacity in a straight line. Ten seats, ten people working a bit faster. Sharing skills compounds instead, because skills stack and circulate.</p>

<ul>
  <li>Every skill one person writes raises the floor for everyone, not just the author.</li>
  <li>Skills improve as more people use them and feed edge cases back.</li>
  <li>A good skill encodes taste and standards, so your team’s way of working spreads without a single meeting.</li>
  <li>New teammates inherit the team’s collected judgment on day one instead of month six.</li>
</ul>

<p>Most orgs frame the goal as “our engineers use AI.” The goal worth aiming for is “our engineers’ agents keep getting better every week without anyone spending extra time on it.” Shared skills are how you get there.</p>

<h2 id="how-to-start">How to Start</h2>

<p>You don’t need a program for this. You need one skill and a place to put it.</p>

<ol>
  <li>Find the task you keep explaining to your agent the same way. That repetition is the signal.</li>
  <li>Write it down as a skill, in whatever format your tooling supports.</li>
  <li>Put it somewhere the team can pull from and edit, the way you already share code.</li>
  <li>Invite people to change it. The first fix from someone else is the moment it stops being yours and becomes infrastructure.</li>
</ol>

<p>Do that once and you’ll feel the difference. Make it a habit and the agents around you keep getting sharper on their own.</p>

<h2 id="conclusion">Conclusion</h2>

<p>The best AI transformation isn’t more seats or a better model. Everyone already has a capable agent. The leverage is in what you teach it, and in whether that teaching stays trapped on one laptop or spreads across the team. Share your skills. Let people sharpen them. Your team’s collective judgment turns into something every agent can run, and that advantage compounds. Happy building!</p>]]></content><author><name>Young Choi</name></author><category term="development" /><category term="ai" /><category term="agents" /><summary type="html"><![CDATA[Most AI adoption plans start with tools and seats. Buy the licenses, run a workshop, tell everyone to use the agent. Adoption climbs for a week, then flattens. Everyone has the same model, and yet the results vary wildly from one person to the next.]]></summary></entry><entry xml:lang="en"><title type="html">How to Use Git Worktrees with Claude Code</title><link href="https://whyjun.github.io/blog/How-to-Use-Git-Worktrees-with-Claude-Code" rel="alternate" type="text/html" title="How to Use Git Worktrees with Claude Code" /><published>2026-07-03T00:00:00-07:00</published><updated>2026-07-03T00:00:00-07:00</updated><id>https://whyjun.github.io/blog/How-to-Use-Git-Worktrees-with-Claude-Code</id><content type="html" xml:base="https://whyjun.github.io/blog/How-to-Use-Git-Worktrees-with-Claude-Code"><![CDATA[<p>When Claude Code runs a long task, it edits files in your working directory. If you want to keep working while it does, you end up fighting over the same files. Git worktrees solve this. They let one repository check out several branches into separate folders at once, so Claude can work in one folder while you stay in another.</p>

<h2 id="what-is-a-git-worktree">What Is a Git Worktree</h2>

<p>A worktree is an extra working directory attached to your existing repository. Each worktree sits on its own branch and has its own files on disk, but they all share one <code class="language-plaintext highlighter-rouge">.git</code> history. You are not cloning the repo again, so you do not pay for a second copy of the object database.</p>

<p>For Claude Code this means isolation. Point Claude at a worktree on a feature branch, and its edits, builds, and test runs stay out of your main checkout.</p>

<h2 id="creating-a-worktree">Creating a Worktree</h2>

<p>Use <code class="language-plaintext highlighter-rouge">git worktree add</code>. Pass a path for the new folder and the branch to check out.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git worktree add ../myproject-feature feature-branch
</code></pre></div></div>

<p>This creates <code class="language-plaintext highlighter-rouge">myproject-feature</code> next to your repo, checked out to <code class="language-plaintext highlighter-rouge">feature-branch</code>. If the branch does not exist yet, create it in the same command with <code class="language-plaintext highlighter-rouge">-b</code>.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git worktree add <span class="nt">-b</span> new-feature ../myproject-feature
</code></pre></div></div>

<p>You now have two folders. Your original one stays where it was. The new one is on <code class="language-plaintext highlighter-rouge">new-feature</code>.</p>

<h2 id="running-claude-code-in-the-worktree">Running Claude Code in the Worktree</h2>

<p>Open a terminal in the worktree folder and start Claude there.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> ../myproject-feature
claude
</code></pre></div></div>

<p>Claude now reads and writes files inside <code class="language-plaintext highlighter-rouge">myproject-feature</code> only. You can go back to your main folder and keep editing, run the app, or start a second Claude session on a different branch. The two sessions never touch the same files.</p>

<p>Claude Code can also manage this for you. When you ask a subagent to work in isolation, it can create a temporary worktree, do its work there, and clean it up when the branch has no changes left.</p>

<h2 id="listing-and-removing-worktrees">Listing and Removing Worktrees</h2>

<p>Check what you have with <code class="language-plaintext highlighter-rouge">git worktree list</code>.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git worktree list
</code></pre></div></div>

<p>When you are done with a worktree, remove it. This deletes the folder and unregisters it from the repo.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git worktree remove ../myproject-feature
</code></pre></div></div>

<p>If you deleted the folder by hand, run <code class="language-plaintext highlighter-rouge">git worktree prune</code> to clear the stale entry.</p>

<h2 id="when-worktrees-help">When Worktrees Help</h2>

<ul>
  <li>Reviewing Claude’s changes on a branch while you keep working on another.</li>
  <li>Running two Claude sessions in parallel on separate features.</li>
  <li>Testing a build without stashing or committing half-finished work.</li>
</ul>

<h2 id="when-they-get-in-the-way">When They Get in the Way</h2>

<ul>
  <li>Small changes where a single branch switch would do.</li>
  <li>Tooling that hardcodes an absolute path and breaks in a second folder.</li>
  <li>Node or build caches that you now have to install once per worktree.</li>
</ul>

<h2 id="conclusion">Conclusion</h2>

<p>Git worktrees give Claude Code a folder of its own without a second clone. Create one with <code class="language-plaintext highlighter-rouge">git worktree add</code>, run Claude inside it, and remove it with <code class="language-plaintext highlighter-rouge">git worktree remove</code> when the branch is merged. If you often want Claude to work while you keep going, keep a spare worktree around and hand it the branch. Happy coding!</p>]]></content><author><name>Young Choi</name></author><category term="development" /><category term="claude-code" /><category term="git" /><summary type="html"><![CDATA[When Claude Code runs a long task, it edits files in your working directory. If you want to keep working while it does, you end up fighting over the same files. Git worktrees solve this. They let one repository check out several branches into separate folders at once, so Claude can work in one folder while you stay in another.]]></summary></entry><entry xml:lang="en"><title type="html">Storing Heterogeneous Objects in PostgreSQL</title><link href="https://whyjun.github.io/blog/Storing-Heterogeneous-Objects-in-PostgreSQL" rel="alternate" type="text/html" title="Storing Heterogeneous Objects in PostgreSQL" /><published>2024-07-08T00:00:00-07:00</published><updated>2024-07-08T00:00:00-07:00</updated><id>https://whyjun.github.io/blog/Storing-Heterogeneous-Objects-in-PostgreSQL</id><content type="html" xml:base="https://whyjun.github.io/blog/Storing-Heterogeneous-Objects-in-PostgreSQL"><![CDATA[<p>Storing different types of objects in one PostgreSQL table is tricky. Each type shares a few fields and carries its own. There are three practical ways to handle it, and each one trades flexibility for structure in a different spot.</p>

<h2 id="scenario-restaurant-order-system">Scenario: Restaurant Order System</h2>

<p>Imagine a <code class="language-plaintext highlighter-rouge">transactions</code> table that stores different types of orders. Each order type has some common fields and some unique ones.</p>

<h3 id="using-jsonb-for-orders">Using JSONB for Orders</h3>

<p>The <code class="language-plaintext highlighter-rouge">JSONB</code> type is flexible. It lets you store different order types in one column without committing to a fixed shape.</p>

<h4 id="schema">Schema</h4>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">transactions</span> <span class="p">(</span>
    <span class="n">id</span> <span class="nb">SERIAL</span> <span class="k">PRIMARY</span> <span class="k">KEY</span><span class="p">,</span>
    <span class="n">orders</span> <span class="n">JSONB</span>
<span class="p">);</span>
</code></pre></div></div>

<h4 id="inserting-data">Inserting Data</h4>

<p>You insert data by writing the orders as JSON.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">transactions</span> <span class="p">(</span><span class="n">orders</span><span class="p">)</span> <span class="k">VALUES</span> <span class="p">(</span>
    <span class="s1">'[
        {"type": "HamburgerOrder", "field1": "value1", "field2": "value2"},
        {"type": "PizzaOrder", "field1": "value1", "field2": "value2", "uniqueField": "uniqueValue"}
    ]'</span><span class="p">::</span><span class="n">jsonb</span>
<span class="p">);</span>
</code></pre></div></div>

<h4 id="querying-data">Querying Data</h4>

<p>Retrieving specific orders stays simple.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="n">transactions</span>
<span class="k">WHERE</span> <span class="n">orders</span> <span class="o">@&gt;</span> <span class="s1">'[{"type": "HamburgerOrder"}]'</span><span class="p">;</span>
</code></pre></div></div>

<h4 id="pros-and-cons">Pros and Cons</h4>

<ul>
  <li>Pros: Very flexible. Simple schema.</li>
  <li>Cons: Complex queries. Limited data validation.</li>
</ul>

<h3 id="using-a-normalized-schema">Using a Normalized Schema</h3>

<p>This method splits the data across several tables joined by foreign keys. It keeps the structure explicit and efficient.</p>

<h4 id="schema-1">Schema</h4>

<p>Transactions Table:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">transactions</span> <span class="p">(</span>
    <span class="n">id</span> <span class="nb">SERIAL</span> <span class="k">PRIMARY</span> <span class="k">KEY</span>
<span class="p">);</span>
</code></pre></div></div>

<p>Orders Table:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">orders</span> <span class="p">(</span>
    <span class="n">id</span> <span class="nb">SERIAL</span> <span class="k">PRIMARY</span> <span class="k">KEY</span><span class="p">,</span>
    <span class="n">transaction_id</span> <span class="nb">INT</span> <span class="k">REFERENCES</span> <span class="n">transactions</span><span class="p">(</span><span class="n">id</span><span class="p">),</span>
    <span class="k">type</span> <span class="nb">VARCHAR</span><span class="p">(</span><span class="mi">50</span><span class="p">)</span> <span class="k">NOT</span> <span class="k">NULL</span><span class="p">,</span>
    <span class="n">common_field1</span> <span class="nb">TEXT</span><span class="p">,</span>
    <span class="n">common_field2</span> <span class="nb">TEXT</span>
<span class="p">);</span>
</code></pre></div></div>

<p>HamburgerOrder Table:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">hamburger_orders</span> <span class="p">(</span>
    <span class="n">id</span> <span class="nb">SERIAL</span> <span class="k">PRIMARY</span> <span class="k">KEY</span><span class="p">,</span>
    <span class="n">order_id</span> <span class="nb">INT</span> <span class="k">REFERENCES</span> <span class="n">orders</span><span class="p">(</span><span class="n">id</span><span class="p">),</span>
    <span class="n">unique_hamburger_field</span> <span class="nb">TEXT</span>
<span class="p">);</span>
</code></pre></div></div>

<p>PizzaOrder Table:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">pizza_orders</span> <span class="p">(</span>
    <span class="n">id</span> <span class="nb">SERIAL</span> <span class="k">PRIMARY</span> <span class="k">KEY</span><span class="p">,</span>
    <span class="n">order_id</span> <span class="nb">INT</span> <span class="k">REFERENCES</span> <span class="n">orders</span><span class="p">(</span><span class="n">id</span><span class="p">),</span>
    <span class="n">unique_pizza_field</span> <span class="nb">TEXT</span>
<span class="p">);</span>
</code></pre></div></div>

<h4 id="inserting-data-1">Inserting Data</h4>

<p>Insert the rows step by step.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">transactions</span> <span class="k">DEFAULT</span> <span class="k">VALUES</span> <span class="n">RETURNING</span> <span class="n">id</span><span class="p">;</span>

<span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">orders</span> <span class="p">(</span><span class="n">transaction_id</span><span class="p">,</span> <span class="k">type</span><span class="p">,</span> <span class="n">common_field1</span><span class="p">,</span> <span class="n">common_field2</span><span class="p">)</span> <span class="k">VALUES</span>
<span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="s1">'HamburgerOrder'</span><span class="p">,</span> <span class="s1">'value1'</span><span class="p">,</span> <span class="s1">'value2'</span><span class="p">)</span> <span class="n">RETURNING</span> <span class="n">id</span><span class="p">;</span>

<span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">hamburger_orders</span> <span class="p">(</span><span class="n">order_id</span><span class="p">,</span> <span class="n">unique_hamburger_field</span><span class="p">)</span> <span class="k">VALUES</span>
<span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="s1">'hamburgerValue'</span><span class="p">);</span>
</code></pre></div></div>

<h4 id="querying-data-1">Querying Data</h4>

<p>Use <code class="language-plaintext highlighter-rouge">JOIN</code> queries to pull the orders back together.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="n">o</span><span class="p">.</span><span class="n">id</span> <span class="k">AS</span> <span class="n">order_id</span><span class="p">,</span> <span class="n">o</span><span class="p">.</span><span class="k">type</span><span class="p">,</span> <span class="n">o</span><span class="p">.</span><span class="n">common_field1</span><span class="p">,</span> <span class="n">o</span><span class="p">.</span><span class="n">common_field2</span><span class="p">,</span>
       <span class="n">h</span><span class="p">.</span><span class="n">unique_hamburger_field</span><span class="p">,</span> <span class="n">p</span><span class="p">.</span><span class="n">unique_pizza_field</span>
<span class="k">FROM</span> <span class="n">orders</span> <span class="n">o</span>
<span class="k">LEFT</span> <span class="k">JOIN</span> <span class="n">hamburger_orders</span> <span class="n">h</span> <span class="k">ON</span> <span class="n">o</span><span class="p">.</span><span class="n">id</span> <span class="o">=</span> <span class="n">h</span><span class="p">.</span><span class="n">order_id</span>
<span class="k">LEFT</span> <span class="k">JOIN</span> <span class="n">pizza_orders</span> <span class="n">p</span> <span class="k">ON</span> <span class="n">o</span><span class="p">.</span><span class="n">id</span> <span class="o">=</span> <span class="n">p</span><span class="p">.</span><span class="n">order_id</span>
<span class="k">WHERE</span> <span class="n">o</span><span class="p">.</span><span class="n">transaction_id</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div>

<h4 id="pros-and-cons-1">Pros and Cons</h4>

<ul>
  <li>Pros: Strong data integrity. Efficient querying.</li>
  <li>Cons: More complex schema. Requires more tables.</li>
</ul>

<h3 id="hybrid-approach-common-fields--jsonb">Hybrid Approach: Common Fields + JSONB</h3>

<p>This approach mixes both. Common fields live in their own columns. Unique fields go into a <code class="language-plaintext highlighter-rouge">JSONB</code> column.</p>

<h4 id="schema-2">Schema</h4>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">orders</span> <span class="p">(</span>
    <span class="n">id</span> <span class="nb">SERIAL</span> <span class="k">PRIMARY</span> <span class="k">KEY</span><span class="p">,</span>
    <span class="n">transaction_id</span> <span class="nb">INT</span> <span class="k">REFERENCES</span> <span class="n">transactions</span><span class="p">(</span><span class="n">id</span><span class="p">),</span>
    <span class="k">type</span> <span class="nb">VARCHAR</span><span class="p">(</span><span class="mi">50</span><span class="p">)</span> <span class="k">NOT</span> <span class="k">NULL</span><span class="p">,</span>
    <span class="n">common_field1</span> <span class="nb">TEXT</span><span class="p">,</span>
    <span class="n">common_field2</span> <span class="nb">TEXT</span><span class="p">,</span>
    <span class="n">specific_fields</span> <span class="n">JSONB</span>
<span class="p">);</span>
</code></pre></div></div>

<h4 id="inserting-data-2">Inserting Data</h4>

<p>Insert the common fields as columns and the rest as JSON.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">INSERT</span> <span class="k">INTO</span> <span class="n">orders</span> <span class="p">(</span><span class="n">transaction_id</span><span class="p">,</span> <span class="k">type</span><span class="p">,</span> <span class="n">common_field1</span><span class="p">,</span> <span class="n">common_field2</span><span class="p">,</span> <span class="n">specific_fields</span><span class="p">)</span> <span class="k">VALUES</span>
<span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="s1">'HamburgerOrder'</span><span class="p">,</span> <span class="s1">'value1'</span><span class="p">,</span> <span class="s1">'value2'</span><span class="p">,</span> <span class="s1">'{"unique_hamburger_field": "hamburgerValue"}'</span><span class="p">::</span><span class="n">jsonb</span><span class="p">),</span>
<span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="s1">'PizzaOrder'</span><span class="p">,</span> <span class="s1">'value1'</span><span class="p">,</span> <span class="s1">'value2'</span><span class="p">,</span> <span class="s1">'{"unique_pizza_field": "pizzaValue"}'</span><span class="p">::</span><span class="n">jsonb</span><span class="p">);</span>
</code></pre></div></div>

<h4 id="querying-data-2">Querying Data</h4>

<p>Query the common fields straight from the columns.</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="o">*</span> <span class="k">FROM</span> <span class="n">orders</span> <span class="k">WHERE</span> <span class="n">transaction_id</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</code></pre></div></div>

<h4 id="pros-and-cons-2">Pros and Cons</h4>

<ul>
  <li>Pros: Flexibility with JSONB. Easier querying for common fields.</li>
  <li>Cons: Validation can be tricky. Slightly more complex.</li>
</ul>

<h2 id="conclusion">Conclusion</h2>

<p>The right method depends on how strict your data has to be. <code class="language-plaintext highlighter-rouge">JSONB</code> is flexible but hard to validate. A normalized schema protects integrity but adds tables and joins. The hybrid keeps common fields queryable while leaving room for the odd ones. Pick the one that matches the shape of your data. Happy coding!</p>]]></content><author><name>Young Choi</name></author><category term="development" /><category term="java" /><category term="sql" /><summary type="html"><![CDATA[Storing different types of objects in one PostgreSQL table is tricky. Each type shares a few fields and carries its own. There are three practical ways to handle it, and each one trades flexibility for structure in a different spot.]]></summary></entry><entry xml:lang="en"><title type="html">Using Interface for Controller Requests in Java Spring Framework</title><link href="https://whyjun.github.io/blog/Using-Interface-for-Controller-Requests-in-Java%20Spring-Framework" rel="alternate" type="text/html" title="Using Interface for Controller Requests in Java Spring Framework" /><published>2024-07-06T00:00:00-07:00</published><updated>2024-07-06T00:00:00-07:00</updated><id>https://whyjun.github.io/blog/Using-Interface-for-Controller-Request</id><content type="html" xml:base="https://whyjun.github.io/blog/Using-Interface-for-Controller-Requests-in-Java%20Spring-Framework"><![CDATA[<p>When you build a REST API with Spring, one endpoint often has to accept several kinds of request. Using an interface for the request body lets a single endpoint handle them all. The example here is a restaurant order system, where one <code class="language-plaintext highlighter-rouge">/v1/orders</code> endpoint takes both hamburger and pizza orders.</p>

<h2 id="scenario-restaurant-order-system">Scenario: Restaurant Order System</h2>

<p>We have an endpoint <code class="language-plaintext highlighter-rouge">/v1/orders</code> that accepts different order types. Every order shares a few fields and adds its own. We define an <code class="language-plaintext highlighter-rouge">Order</code> interface and implement it with one class per order type.</p>

<h3 id="step-1-define-the-order-interface">Step 1: Define the Order Interface</h3>

<p>Start with an <code class="language-plaintext highlighter-rouge">Order</code> interface holding what every order has in common.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">Order</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="nf">getOrderType</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="step-2-implement-order-types">Step 2: Implement Order Types</h3>

<p>Then write a class per order type, like <code class="language-plaintext highlighter-rouge">HamburgerOrder</code> and <code class="language-plaintext highlighter-rouge">PizzaOrder</code>.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">lombok.Data</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">javax.validation.constraints.NotNull</span><span class="o">;</span>

<span class="nd">@Data</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">HamburgerOrder</span> <span class="kd">implements</span> <span class="nc">Order</span> <span class="o">{</span>
    <span class="nd">@NotNull</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">orderType</span> <span class="o">=</span> <span class="s">"hamburger"</span><span class="o">;</span>
    <span class="nd">@NotNull</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">bunType</span><span class="o">;</span>
    <span class="nd">@NotNull</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">meatType</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">boolean</span> <span class="n">extraCheese</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">lombok.Data</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">javax.validation.constraints.NotNull</span><span class="o">;</span>

<span class="nd">@Data</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">PizzaOrder</span> <span class="kd">implements</span> <span class="nc">Order</span> <span class="o">{</span>
    <span class="nd">@NotNull</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">orderType</span> <span class="o">=</span> <span class="s">"pizza"</span><span class="o">;</span>
    <span class="nd">@NotNull</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">crustType</span><span class="o">;</span>
    <span class="nd">@NotNull</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">size</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kt">boolean</span> <span class="n">extraToppings</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="step-3-create-the-controller">Step 3: Create the Controller</h3>

<p>The controller handles the <code class="language-plaintext highlighter-rouge">/v1/orders</code> POST request. The <code class="language-plaintext highlighter-rouge">@RequestBody</code> and <code class="language-plaintext highlighter-rouge">@Valid</code> annotations bind and validate the incoming JSON.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.springframework.http.HttpStatus</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.http.ResponseEntity</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.validation.annotation.Validated</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.*</span><span class="o">;</span>

<span class="kn">import</span> <span class="nn">javax.validation.Valid</span><span class="o">;</span>

<span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/v1/orders"</span><span class="o">)</span>
<span class="nd">@Validated</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">OrderController</span> <span class="o">{</span>

    <span class="nd">@PostMapping</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="nf">createOrder</span><span class="o">(</span><span class="nd">@Valid</span> <span class="nd">@RequestBody</span> <span class="nc">Order</span> <span class="n">order</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">order</span> <span class="k">instanceof</span> <span class="nc">HamburgerOrder</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">HamburgerOrder</span> <span class="n">hamburgerOrder</span> <span class="o">=</span> <span class="o">(</span><span class="nc">HamburgerOrder</span><span class="o">)</span> <span class="n">order</span><span class="o">;</span>
            <span class="c1">// Process hamburger order</span>
            <span class="k">return</span> <span class="k">new</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="s">"Hamburger order received"</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">OK</span><span class="o">);</span>
        <span class="o">}</span> <span class="k">else</span> <span class="k">if</span> <span class="o">(</span><span class="n">order</span> <span class="k">instanceof</span> <span class="nc">PizzaOrder</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">PizzaOrder</span> <span class="n">pizzaOrder</span> <span class="o">=</span> <span class="o">(</span><span class="nc">PizzaOrder</span><span class="o">)</span> <span class="n">order</span><span class="o">;</span>
            <span class="c1">// Process pizza order</span>
            <span class="k">return</span> <span class="k">new</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="s">"Pizza order received"</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">OK</span><span class="o">);</span>
        <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
            <span class="k">return</span> <span class="k">new</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="s">"Unknown order type"</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">BAD_REQUEST</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="step-4-configure-jackson-for-polymorphic-deserialization">Step 4: Configure Jackson for Polymorphic Deserialization</h3>

<p>Jackson needs to know which class to build from the JSON. Annotate the <code class="language-plaintext highlighter-rouge">Order</code> interface so it reads the <code class="language-plaintext highlighter-rouge">orderType</code> field and picks the matching implementation.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">com.fasterxml.jackson.annotation.JsonSubTypes</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">com.fasterxml.jackson.annotation.JsonTypeInfo</span><span class="o">;</span>

<span class="nd">@JsonTypeInfo</span><span class="o">(</span>
        <span class="n">use</span> <span class="o">=</span> <span class="nc">JsonTypeInfo</span><span class="o">.</span><span class="na">Id</span><span class="o">.</span><span class="na">NAME</span><span class="o">,</span>
        <span class="n">include</span> <span class="o">=</span> <span class="nc">JsonTypeInfo</span><span class="o">.</span><span class="na">As</span><span class="o">.</span><span class="na">PROPERTY</span><span class="o">,</span>
        <span class="n">property</span> <span class="o">=</span> <span class="s">"orderType"</span>
<span class="o">)</span>
<span class="nd">@JsonSubTypes</span><span class="o">({</span>
        <span class="nd">@JsonSubTypes</span><span class="o">.</span><span class="na">Type</span><span class="o">(</span><span class="n">value</span> <span class="o">=</span> <span class="nc">HamburgerOrder</span><span class="o">.</span><span class="na">class</span><span class="o">,</span> <span class="n">name</span> <span class="o">=</span> <span class="s">"hamburger"</span><span class="o">),</span>
        <span class="nd">@JsonSubTypes</span><span class="o">.</span><span class="na">Type</span><span class="o">(</span><span class="n">value</span> <span class="o">=</span> <span class="nc">PizzaOrder</span><span class="o">.</span><span class="na">class</span><span class="o">,</span> <span class="n">name</span> <span class="o">=</span> <span class="s">"pizza"</span><span class="o">)</span>
<span class="o">})</span>
<span class="kd">public</span> <span class="kd">interface</span> <span class="nc">Order</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="nf">getOrderType</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="step-5-add-global-validation-handling">Step 5: Add Global Validation Handling</h3>

<p>Finally, handle validation errors in one place.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">org.springframework.http.HttpStatus</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.http.ResponseEntity</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.MethodArgumentNotValidException</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.ControllerAdvice</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">org.springframework.web.bind.annotation.ExceptionHandler</span><span class="o">;</span>

<span class="nd">@ControllerAdvice</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">GlobalExceptionHandler</span> <span class="o">{</span>

    <span class="nd">@ExceptionHandler</span><span class="o">(</span><span class="nc">MethodArgumentNotValidException</span><span class="o">.</span><span class="na">class</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="nf">handleValidationExceptions</span><span class="o">(</span><span class="nc">MethodArgumentNotValidException</span> <span class="n">ex</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="k">new</span> <span class="nc">ResponseEntity</span><span class="o">&lt;&gt;(</span><span class="s">"Invalid order data"</span><span class="o">,</span> <span class="nc">HttpStatus</span><span class="o">.</span><span class="na">BAD_REQUEST</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="conclusion">Conclusion</h2>

<p>An interface for the request body lets one endpoint accept many order types without a pile of branching at the door. We defined an <code class="language-plaintext highlighter-rouge">Order</code> interface, implemented it per type, let Jackson pick the right class from <code class="language-plaintext highlighter-rouge">orderType</code>, and validated with <code class="language-plaintext highlighter-rouge">@Valid</code>. Adding a new order type now means writing one class and one Jackson subtype, and nothing else has to change. Happy coding!</p>]]></content><author><name>Young Choi</name></author><category term="development" /><category term="java" /><summary type="html"><![CDATA[When you build a REST API with Spring, one endpoint often has to accept several kinds of request. Using an interface for the request body lets a single endpoint handle them all. The example here is a restaurant order system, where one /v1/orders endpoint takes both hamburger and pizza orders.]]></summary></entry><entry xml:lang="en"><title type="html">The Importance of Signals from Test Codes</title><link href="https://whyjun.github.io/blog/The-Importance-of-Signals-from-Test-Codes" rel="alternate" type="text/html" title="The Importance of Signals from Test Codes" /><published>2024-06-30T00:00:00-07:00</published><updated>2024-06-30T00:00:00-07:00</updated><id>https://whyjun.github.io/blog/The%20Importance-of-Signals-from-Test-Codes</id><content type="html" xml:base="https://whyjun.github.io/blog/The-Importance-of-Signals-from-Test-Codes"><![CDATA[<p>When you write tests, they tell you a lot about your main code. If a test is hard to write, the problem is usually in the code you are testing, not in the test. Those difficulties are signals, and they are worth reading.</p>

<h2 id="why-test-codes-matter">Why Test Codes Matter</h2>

<p>Tests are a mirror for your main code. They show its strengths and its weak spots. A few signals to watch for:</p>

<ul>
  <li>Difficulty writing tests: the code might be too complex or not modular.</li>
  <li>Excessive mocking: the code might be tightly coupled.</li>
  <li>Long setup times: the code might carry too many dependencies.</li>
  <li>Hidden dependencies: the code might be hard to maintain or extend.</li>
</ul>

<h2 id="key-signals-and-what-they-mean">Key Signals and What They Mean</h2>

<h3 id="excessive-mocking">Excessive Mocking</h3>

<p>Mocking replaces a real dependency with a fake one. A few mocks are normal. When a single test needs many of them, the class under test leans on too many other classes. The fix is in the design: break the large class into smaller ones that can stand on their own.</p>

<h3 id="long-setup-times">Long Setup Times</h3>

<p>Setting up a test should be quick. When it drags, the code usually has too many dependencies or too little structure. Dependency injection helps here, since the test can supply what it needs instead of building the whole world first.</p>

<h3 id="difficulty-isolating-tests">Difficulty Isolating Tests</h3>

<p>Each test should run on its own. When one test changes the outcome of another, the code has shared state hiding somewhere. Make each unit do one thing so the tests stop leaking into each other.</p>

<h3 id="hidden-dependencies">Hidden Dependencies</h3>

<p>Some dependencies are not visible in a method’s signature. Calling <code class="language-plaintext highlighter-rouge">Instant.now()</code> inside a method is one of them: the method quietly depends on the system clock, so you cannot test it without controlling time. Dependency inversion fixes this. Instead of reading the clock directly, inject a clock interface and ask it for the current time.</p>

<h4 id="implementing-dependency-inversion">Implementing Dependency Inversion</h4>

<p>Here is the same idea in code.</p>

<p>Before:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">Event</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">Instant</span> <span class="n">timestamp</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">Event</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">timestamp</span> <span class="o">=</span> <span class="nc">Instant</span><span class="o">.</span><span class="na">now</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>After:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">interface</span> <span class="nc">Clock</span> <span class="o">{</span>
    <span class="nc">Instant</span> <span class="nf">now</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">SystemClock</span> <span class="kd">implements</span> <span class="nc">Clock</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="nc">Instant</span> <span class="nf">now</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="nc">Instant</span><span class="o">.</span><span class="na">now</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">FakeClock</span> <span class="kd">implements</span> <span class="nc">Clock</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="nc">Instant</span> <span class="nf">now</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="nc">Instant</span><span class="o">.</span><span class="na">parse</span><span class="o">(</span><span class="s">"2007-12-03T10:15:30.00Z"</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Event</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="nc">Instant</span> <span class="n">timestamp</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">Event</span><span class="o">(</span><span class="nc">Clock</span> <span class="n">clock</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">timestamp</span> <span class="o">=</span> <span class="n">clock</span><span class="o">.</span><span class="na">now</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h2 id="benefits-of-listening-to-these-signals">Benefits of Listening to These Signals</h2>

<p>Reading these signals improves the design. Modular code is easier to follow, has fewer places for bugs to hide, and takes less effort to extend when the requirements change.</p>

<h2 id="practical-steps-to-improve-your-code">Practical Steps to Improve Your Code</h2>

<ul>
  <li>Refactor regularly. Don’t wait until the code is a mess. Clean it up as you go.</li>
  <li>Use dependency injection. It reduces the need for mocks and keeps the code modular.</li>
  <li>Write small, focused functions. Each one should do a single thing, which makes it easy to test.</li>
  <li>Find and remove hidden dependencies. Patterns like port-adapter and dependency inversion decouple your code.</li>
</ul>

<h2 id="conclusion">Conclusion</h2>

<p>Tests do more than check that your code works. They point at where the design is weak. When a test is painful to write, listen to it and fix the code underneath instead of forcing the test around it. Keep your tests simple, and let them guide the design.</p>]]></content><author><name>Young Choi</name></author><category term="development" /><category term="test" /><summary type="html"><![CDATA[When you write tests, they tell you a lot about your main code. If a test is hard to write, the problem is usually in the code you are testing, not in the test. Those difficulties are signals, and they are worth reading.]]></summary></entry><entry xml:lang="ko"><title type="html">Go에서 omitempty 태그 사용하기</title><link href="https://whyjun.github.io/golang/Golang-Omitempty-Tag" rel="alternate" type="text/html" title="Go에서 omitempty 태그 사용하기" /><published>2023-03-15T00:00:00-07:00</published><updated>2023-03-15T00:00:00-07:00</updated><id>https://whyjun.github.io/golang/Golang-Omitempty-Tag</id><content type="html" xml:base="https://whyjun.github.io/golang/Golang-Omitempty-Tag"><![CDATA[<p>Go로 웹 서버를 개발하다 보면 JSON 데이터를 주고 받는 경우가 많이 있습니다. struct를 JSON으로 바꿀 때 값이 Zero Value(각 type별 기본 값)이거나 nil인 경우 그 값을 보내지 않아야할 때가 있습니다. 이런 경우, <code class="language-plaintext highlighter-rouge">omitempty</code> 태그를 활용하면 JSON에서 쉽게 생략할 수 있습니다.</p>

<p>struct 필드에 <code class="language-plaintext highlighter-rouge">omitempty</code> 태그가 있다면, Go 언어의 JSON 인코더는 그 필드의 값이 Zero Value거나 nil일 때 그 필드를 생략합니다. 이는 JSON으로 데이터를 보내고 받는 API를 개발할 때 유용하고 특히 JSON 메세지의 사이즈를 줄일 수 있다는 장점이 있습니다.</p>

<h2 id="omitempty-태그">omitempty 태그</h2>

<p>struct의 <code class="language-plaintext highlighter-rouge">omitempty</code> 태그는 아래와 같이 필드 type 뒤에 Backtick(`)안에 작성해 추가할 수 있습니다.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">type</span> <span class="n">Person</span> <span class="k">struct</span> <span class="p">{</span>
    <span class="n">Name</span>      <span class="kt">string</span> <span class="s">`json:"name,omitempty"`</span>
    <span class="n">Age</span>       <span class="kt">int</span>    <span class="s">`json:"age,omitempty"`</span>
    <span class="n">Address</span>   <span class="kt">string</span> <span class="s">`json:"address,omitempty"`</span>
    <span class="n">Telephone</span> <span class="kt">string</span> <span class="s">`json:"telephone,omitempty"`</span>
<span class="p">}</span>
</code></pre></div></div>

<p>위의 예시를 보면 모든 필드에 <code class="language-plaintext highlighter-rouge">omitempty</code> 태그가 있는 것을 볼 수 있습니다. 이는 필드의 값이 Zero Value라면 JSON 형식으로 데이터를 변경할 때 필드가 사라짐을 의미합니다. 예를 들어 아래의 값을 가진 <code class="language-plaintext highlighter-rouge">Person</code> 인스턴스가 있다고 합시다.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">p</span> <span class="o">:=</span> <span class="n">Person</span><span class="p">{</span>
    <span class="n">Name</span><span class="o">:</span>      <span class="s">"John"</span><span class="p">,</span>
    <span class="n">Age</span><span class="o">:</span>       <span class="m">0</span><span class="p">,</span>
    <span class="n">Address</span><span class="o">:</span>   <span class="s">""</span><span class="p">,</span>
    <span class="n">Telephone</span><span class="o">:</span> <span class="s">"123-456-7890"</span><span class="p">,</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Person</code>을 <code class="language-plaintext highlighter-rouge">json.Marshal</code> 함수를 사용해서 JSON으로 바꾼다면 결과값은 아래와 같이 나옵니다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="nl">"name"</span><span class="p">:</span><span class="w"> </span><span class="s2">"John"</span><span class="p">,</span><span class="w">
    </span><span class="nl">"telephone"</span><span class="p">:</span><span class="w"> </span><span class="s2">"123-456-7890"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div>

<p>위에서 볼 수 있듯이, <code class="language-plaintext highlighter-rouge">Age</code>와 <code class="language-plaintext highlighter-rouge">Address</code> 필드가 사라진 것을 확인할 수 있습니다.</p>]]></content><author><name>Young Choi</name></author><category term="Golang" /><category term="golang" /><summary type="html"><![CDATA[Go로 웹 서버를 개발하다 보면 JSON 데이터를 주고 받는 경우가 많이 있습니다. struct를 JSON으로 바꿀 때 값이 Zero Value(각 type별 기본 값)이거나 nil인 경우 그 값을 보내지 않아야할 때가 있습니다. 이런 경우, omitempty 태그를 활용하면 JSON에서 쉽게 생략할 수 있습니다.]]></summary></entry><entry xml:lang="ko"><title type="html">golangcli-lint로 Go 코드 규칙 적용하기</title><link href="https://whyjun.github.io/golang/Use-GolangCI-Lint" rel="alternate" type="text/html" title="golangcli-lint로 Go 코드 규칙 적용하기" /><published>2023-03-08T00:00:00-08:00</published><updated>2023-03-08T00:00:00-08:00</updated><id>https://whyjun.github.io/golang/Use-GolangCI-Lint</id><content type="html" xml:base="https://whyjun.github.io/golang/Use-GolangCI-Lint"><![CDATA[<p>개발팀 사이즈가 커지면서 관리해야 하는 리포지토리의 개수가 늘어났고 각각의 프로젝트가 팀을 리드하는 개발자에 따라 자유분방한 모습으로 진화했습니다. 특히 Go 언어를 처음으로 사용하는 개발자들이 늘어나면서 코드 리뷰 과정에서 Idiomatic Go에 대해서 설명하고 작성하는데 시간을 꽤 소모하게 되었습니다. 그러다보니 실제로 리뷰가 되어야 하는 부분보다 더 많은 시간을 쏟게 되는 경우가 늘어났고 이를 방지하기 위해 자동화된 도구가 필요하다라는 피드백이 나오기 시작했습니다.</p>

<p>다행히도 Go 커뮤니티에는 다양한 Lint 도구들이 이미 개발되어 있었는데 대표적으로 <code class="language-plaintext highlighter-rouge">govet</code>, <code class="language-plaintext highlighter-rouge">gofmt</code>, <code class="language-plaintext highlighter-rouge">golint</code> 등이 있습니다. 이러한 도구들은 좋은 Go 코드를 작성하기 위해 필요한 부분들을 다 체크하지만 각각 따로 돌려야 한다는 단점이 있습니다. 이러한 단점을 극복하기 위해 저희 팀은 <code class="language-plaintext highlighter-rouge">golangci-lint</code>를 사용하기로 했습니다.</p>

<p><code class="language-plaintext highlighter-rouge">golangci-lint</code>는 Lint 도구는 아니지만 다양한 Lint 도구들을 한번에 돌릴 수 있게 도와줍니다. 단순히 도구들을 순서대로 돌리는 것이 아니라 병렬적으로 돌리는 동시에 분석 결과값을 캐싱함으로써 정적 분석을 매우 빠르게 끝낼 수 있습니다. 그리고 회사에 많은 개발자들이 사용하고 있는 Visual Studio Code를 지원하고 이 점을 활용해 <code class="language-plaintext highlighter-rouge">settings.json</code>에 설정 몇 가지를 추가하는 것으로 코드 규칙을 지키지 않을 경우 Warning 메세지를 바로 띄워주어 개발자들이 빠르게 고칠 수 있게 도와줍니다.</p>

<p><img src="https://repository-images.githubusercontent.com/132145189/05239680-dfaf-11e9-9646-2c3ef2f5f8d4" alt="golangci-lint log" class="align-center" /></p>

<h2 id="설치하기">설치하기</h2>

<p>Mac과 Windows가 아닌 다른 환경에서 설치하는 방법은 <a href="https://golangci-lint.run/usage/install/">공식 문서</a>에 자세히 나와있습니다.</p>

<h3 id="mac">Mac</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>golangci-lint
brew upgrade golangci-lint

golangci-lint version
</code></pre></div></div>

<h3 id="windows">Windows</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Git for Windows를 설치할 때 같이 설치되는 Git Bash에 아래 명령어를 입력하시면 설치됩니다.</span>
<span class="c"># 실제 binary는 $(go env GOPATH)/bin/golangci-lint에 있습니다</span>
curl <span class="nt">-sSfL</span> https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh <span class="nt">-s</span> <span class="nt">--</span> <span class="nt">-b</span> <span class="si">$(</span>go <span class="nb">env </span>GOPATH<span class="si">)</span>/bin 

golangci-lint <span class="nt">--version</span>
</code></pre></div></div>

<h2 id="사용하기">사용하기</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>golangci-lint run
</code></pre></div></div>

<p>위 명령어를 입력하면 <code class="language-plaintext highlighter-rouge">golangci-lint</code> 기본으로 설정되어 있는 Lint 도구들로 코드를 분석합니다. 어떠한 도구들을 사용하는지 확인하고 싶으면 <code class="language-plaintext highlighter-rouge">golangci-lint help linters</code>로 확인할 수 있습니다. <code class="language-plaintext highlighter-rouge">golangci-lint</code>를 직접 설정하고 싶다면 <a href="https://golangci-lint.run/usage/configuration/">공식 문서</a>를 참고해서 <code class="language-plaintext highlighter-rouge">.golangci.yml</code> 파일을 작성하시면 됩니다.</p>

<h2 id="visual-studio-code에-추가하기">Visual Studio Code에 추가하기</h2>

<p>Visual Studio Code 설정에 들어가서 <code class="language-plaintext highlighter-rouge">settings.json</code>을 연 다음, 아래 설정을 추가하면 Visual Studio Code에서 Warning 메세지를 확인할 수 있습니다.</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">"go.lintTool"</span><span class="p">:</span><span class="w"> </span><span class="s2">"golangci-lint"</span><span class="w">
</span><span class="nl">"go.lintFlags"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="w">
    </span><span class="s2">"--fast"</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre></div></div>

<h2 id="사용-후기">사용 후기</h2>

<p>실제로 적용해보니 코드 리뷰로 잡아내지 못한 이슈들이 꽤 있었습니다. 대표적으로 error를 <code class="language-plaintext highlighter-rouge">fmt.Errorf</code>로 래핑할 때, <code class="language-plaintext highlighter-rouge">%w</code>를 사용하지 않았던 부분입니다. <code class="language-plaintext highlighter-rouge">errors</code> 패키지를 잘 활용하지 않았다보니 알아차리지 못했지만 프로젝트의 사이즈가 커지면서 어떤 error인지를 확인해야 하는 경우가 빈번하게 발생했는데 <code class="language-plaintext highlighter-rouge">%w</code>가 아닌 <code class="language-plaintext highlighter-rouge">%s</code>나 <code class="language-plaintext highlighter-rouge">%v</code>를 사용했을 때는 <code class="language-plaintext highlighter-rouge">errors.UnWrap()</code> 함수를 사용할 수 없고 그 때문에 <code class="language-plaintext highlighter-rouge">errors.Is</code>와 <code class="language-plaintext highlighter-rouge">errors.As</code>도 활용할 수 없다는 것을 알게 되었습니다. 이처럼 Linter는 하나의 정해진 코드 규칙을 모든 개발자가 지킬 수 있게 해준다는 점을 넘어서 코드에 있는 버그들을 개발자들이 일찍 알아차릴 수 있게 해줍니다.</p>]]></content><author><name>Young Choi</name></author><category term="Golang" /><category term="golang" /><summary type="html"><![CDATA[개발팀 사이즈가 커지면서 관리해야 하는 리포지토리의 개수가 늘어났고 각각의 프로젝트가 팀을 리드하는 개발자에 따라 자유분방한 모습으로 진화했습니다. 특히 Go 언어를 처음으로 사용하는 개발자들이 늘어나면서 코드 리뷰 과정에서 Idiomatic Go에 대해서 설명하고 작성하는데 시간을 꽤 소모하게 되었습니다. 그러다보니 실제로 리뷰가 되어야 하는 부분보다 더 많은 시간을 쏟게 되는 경우가 늘어났고 이를 방지하기 위해 자동화된 도구가 필요하다라는 피드백이 나오기 시작했습니다.]]></summary></entry></feed>